---
phase: 25-bot-player
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/types/room.ts
  - packages/shared/src/schemas/messages.ts
  - packages/shared/src/types/messages.ts
  - packages/server/src/rooms/Room.ts
  - packages/server/src/rooms/RoomManager.ts
  - packages/server/tests/rooms/Room.test.ts
autonomous: true

must_haves:
  truths:
    - "LobbyPlayer type includes optional isBot boolean field"
    - "Room can add a bot with a unique bot_ prefixed ID"
    - "Room can remove a bot by its ID"
    - "Room.isBot() correctly identifies bot player IDs"
    - "Bots cannot be added after game starts or when room is full"
    - "add-bot and remove-bot client message schemas exist and validate correctly"
    - "Room.getState() returns isBot flag on bot LobbyPlayer entries"
  artifacts:
    - path: "packages/shared/src/types/room.ts"
      provides: "LobbyPlayer.isBot field"
      contains: "isBot"
    - path: "packages/shared/src/schemas/messages.ts"
      provides: "add-bot and remove-bot schemas in clientMessageSchema"
      contains: "add-bot"
    - path: "packages/server/src/rooms/Room.ts"
      provides: "addBot, removeBot, isBot, getBotIds methods"
      contains: "addBot"
  key_links:
    - from: "packages/server/src/rooms/Room.ts"
      to: "packages/shared/src/types/room.ts"
      via: "LobbyPlayer isBot field used in getState()"
      pattern: "isBot"
---

<objective>
Add bot player infrastructure to the shared types and Room class.

Purpose: Establish the foundational types, message schemas, and Room methods needed for bot player support. This is the data layer that all other bot plans build on.
Output: Updated shared types with isBot support, new message schemas for add-bot/remove-bot, Room class with bot management methods, and unit tests.
</objective>

<execution_context>
@/Users/stephendickson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stephendickson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-bot-player/25-RESEARCH.md
@packages/shared/src/types/room.ts
@packages/shared/src/schemas/messages.ts
@packages/shared/src/types/messages.ts
@packages/server/src/rooms/Room.ts
@packages/server/src/rooms/RoomManager.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add isBot to shared types and message schemas</name>
  <files>
    packages/shared/src/types/room.ts
    packages/shared/src/schemas/messages.ts
    packages/shared/src/types/messages.ts
  </files>
  <action>
    1. In `packages/shared/src/types/room.ts`:
       - Add `isBot?: boolean` field to `LobbyPlayer` type (after discordUserId)

    2. In `packages/shared/src/schemas/messages.ts`:
       - Add `isBot: z.boolean().optional()` to `lobbyPlayerSchema`
       - Add `addBotSchema`:
         ```typescript
         export const addBotSchema = z.object({
           type: z.literal('add-bot'),
           nickname: z.string().min(1).max(20).trim().optional(),
         });
         ```
       - Add `removeBotSchema`:
         ```typescript
         export const removeBotSchema = z.object({
           type: z.literal('remove-bot'),
           botId: z.string(),
         });
         ```
       - Add both schemas to `clientMessageSchema` discriminated union
       - Do NOT add new server message schemas — room-updated already broadcasts the updated room state with isBot

    3. In `packages/shared/src/types/messages.ts`:
       - Add type exports for the new schemas:
         ```typescript
         export type AddBotMessage = z.infer<typeof addBotSchema>;
         export type RemoveBotMessage = z.infer<typeof removeBotSchema>;
         ```
       - Add imports for addBotSchema, removeBotSchema at the top
  </action>
  <verify>
    Run `cd /Users/stephendickson/Personal/shit-head && make type-check` — should pass with no errors.
  </verify>
  <done>
    LobbyPlayer has isBot field, add-bot and remove-bot schemas exist in clientMessageSchema, corresponding TypeScript types are exported.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add bot management methods to Room and RoomManager</name>
  <files>
    packages/server/src/rooms/Room.ts
    packages/server/src/rooms/RoomManager.ts
    packages/server/tests/rooms/Room.test.ts
  </files>
  <action>
    1. In `packages/server/src/rooms/Room.ts`:
       - Add a private field: `private botPlayerIds: Set<string> = new Set();`
       - Add a private field for bot name counter: `private botNameCounter: number = 0;`
       - Add `addBot(botNickname?: string): OperationResult<string>` method:
         - If `this.status !== 'waiting'`, return error INVALID_ACTION
         - If `this.players.size >= this.maxPlayers`, return error ROOM_FULL
         - Generate bot ID: `const botId = 'bot_' + generateRoomCode();` (reuse the nanoid generator already imported)
         - Actually, use a separate nanoid for bot IDs. Import `nanoid` from 'nanoid' at the top: `import { nanoid } from 'nanoid';` and use `const botId = 'bot_' + nanoid(8);`
         - If no nickname provided, auto-generate: `const nickname = botNickname || 'Bot ' + (++this.botNameCounter);`
         - Add to `this.players` Map with `isBot: true` and `isHost: false`, `avatarHash: null`, `discordUserId: null`
         - Add to `this.botPlayerIds` Set
         - Return `{ success: true, data: botId }`
       - Add `removeBot(botId: string): OperationResult` method:
         - If `!this.botPlayerIds.has(botId)`, return error PLAYER_NOT_FOUND
         - Delete from `this.players` and `this.botPlayerIds`
         - Return `{ success: true }`
       - Add `isBot(playerId: string): boolean` method:
         - Return `this.botPlayerIds.has(playerId)`
       - Add `getBotIds(): string[]` method:
         - Return `Array.from(this.botPlayerIds)`
       - Modify `getState()` method to include `isBot` in the `LobbyPlayer` mapping:
         ```typescript
         const players: LobbyPlayer[] = Array.from(this.players.values()).map(p => ({
           id: p.id,
           nickname: p.nickname,
           isHost: p.isHost,
           avatarHash: p.avatarHash ?? null,
           discordUserId: p.discordUserId ?? null,
           isBot: this.botPlayerIds.has(p.id) || undefined,
         }));
         ```
         Use `|| undefined` so that the field is omitted for non-bots (keeps payloads clean).
       - Update the internal player type in the `players` Map to NOT add isBot there — isBot tracking is done via the `botPlayerIds` Set, not per-player object fields.
       - In `resetToLobby()`: After clearing game state, also remove all bots:
         ```typescript
         // Remove bots on return to lobby
         for (const botId of this.botPlayerIds) {
           this.players.delete(botId);
         }
         this.botPlayerIds.clear();
         this.botNameCounter = 0;
         ```
       - In `autoReturnToLobby()`: Same — remove all bots before promoting spectators.

    2. In `packages/server/src/rooms/RoomManager.ts`:
       - No changes needed — bots are managed at Room level. The RoomManager doesn't need to know about bots specifically.

    3. In `packages/server/tests/rooms/Room.test.ts`:
       - Add a new describe block `'Bot management'` with these tests:
         - `addBot adds a bot player with bot_ prefix ID`
         - `addBot auto-generates nickname when none provided`
         - `addBot uses provided nickname`
         - `addBot fails when room is full`
         - `addBot fails when game already started`
         - `removeBot removes a bot player`
         - `removeBot fails for non-bot player`
         - `isBot returns true for bot, false for human`
         - `getBotIds returns all bot IDs`
         - `getState includes isBot for bot players`
         - `resetToLobby removes all bots`
       - Each test creates a Room instance and exercises the bot methods directly.
  </action>
  <verify>
    Run `cd /Users/stephendickson/Personal/shit-head && make test-server` — all tests pass including new bot tests.
    Run `cd /Users/stephendickson/Personal/shit-head && make lint` — zero lint errors.
  </verify>
  <done>
    Room has addBot/removeBot/isBot/getBotIds methods, bots appear in getState() with isBot flag, bots are cleaned up on resetToLobby, all tests pass.
  </done>
</task>

</tasks>

<verification>
1. `make type-check` passes
2. `make test-server` passes (including new bot management tests)
3. `make lint` passes
</verification>

<success_criteria>
- LobbyPlayer type has isBot field
- add-bot and remove-bot message schemas validated by Zod
- Room class can add/remove/identify bots
- Room.getState() includes isBot for bot players
- Bots cleaned up on resetToLobby/autoReturnToLobby
- All server tests pass
- Lint passes
</success_criteria>

<output>
After completion, create `.planning/phases/25-bot-player/25-01-SUMMARY.md`
</output>
