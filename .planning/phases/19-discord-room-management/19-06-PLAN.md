---
phase: 19-discord-room-management
plan: 06
type: execute
wave: 4
depends_on: ["19-02", "19-03", "19-04", "19-05"]
files_modified:
  - DISCORD-SETUP.md
  - packages/server/src/routes/api.ts
autonomous: true

must_haves:
  truths:
    - "cloudflared tunnel setup documented for local Discord Activity testing"
    - "Production deployment steps documented (Render env vars, Discord Developer Portal URL mappings)"
    - "Cookie configuration for Discord third-party iframe documented"
    - "All server and client type checks pass"
    - "All server tests pass"
    - "Linting passes"
  artifacts:
    - path: "DISCORD-SETUP.md"
      provides: "Complete Discord Activity setup documentation"
      contains: "cloudflared"
  key_links:
    - from: "DISCORD-SETUP.md"
      to: "packages/server/src/routes/api.ts"
      via: "Token endpoint documentation"
      pattern: "api/token"
---

<objective>
Update deployment documentation for Discord Activity, verify production cookie configuration, and run full verification of all Phase 19 changes.

Purpose: Phase 19 success criteria require cloudflared tunnel documentation, production deployment docs, and cookie SameSite configuration for Discord's iframe. This plan also serves as the final integration verification for all prior plans.

Output: Updated DISCORD-SETUP.md with production deployment steps and cloudflared tunnel instructions. Cookie configuration verified. All tests and type checks passing.
</objective>

<execution_context>
@/Users/stephendickson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stephendickson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-discord-room-management/19-01-SUMMARY.md
@.planning/phases/19-discord-room-management/19-02-SUMMARY.md
@.planning/phases/19-discord-room-management/19-03-SUMMARY.md
@.planning/phases/19-discord-room-management/19-04-SUMMARY.md
@.planning/phases/19-discord-room-management/19-05-SUMMARY.md
@DISCORD-SETUP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update DISCORD-SETUP.md with production deployment and cookie docs</name>
  <files>DISCORD-SETUP.md</files>
  <action>
Read the existing DISCORD-SETUP.md (created in Phase 16) and add/update the following sections:

**1. Production Deployment section:**
```markdown
## Production Deployment

### Environment Variables

Set these in your hosting provider (Render):

| Variable | Where | Value |
|----------|-------|-------|
| `DISCORD_CLIENT_ID` | Server | From Discord Developer Portal |
| `DISCORD_CLIENT_SECRET` | Server | From Discord Developer Portal |
| `VITE_DISCORD_CLIENT_ID` | Client build | Same as DISCORD_CLIENT_ID |

### Discord Developer Portal Configuration

1. Go to Applications -> Your App -> Activities
2. Under URL Mappings, configure:
   - **Root mapping**: Prefix `/` -> Target `your-production-domain.com`
   - **Proxy mapping**: Prefix `/.proxy` -> Target `your-production-domain.com/.proxy`
3. Under Supported Platforms, enable Desktop and Web

### Cookie Configuration

If your server introduces cookies (session, auth), configure them for Discord's third-party iframe context:

```
Set-Cookie: name=value; SameSite=None; Partitioned; Secure; Path=/
```

- `SameSite=None` — required for cross-origin iframe (Discord embeds your app)
- `Partitioned` — required by Chrome's third-party cookie deprecation (CHIPS)
- `Secure` — required when using SameSite=None

Currently, the Shithead server does NOT use cookies. Discord auth uses memory-only access tokens. This section is for future reference if cookies are added.
```

**2. Cloudflared Tunnel section (verify/update existing):**
Ensure the existing cloudflared documentation covers:
- `make dev-discord` command
- How to find the tunnel URL in docker-compose output
- How to configure the tunnel URL in Discord Developer Portal -> Activities -> URL Mappings
- Testing flow: start tunnel -> update URL mapping -> open Discord -> launch Activity

**3. Phase 19 Auto-Join section:**
```markdown
## Discord Room Management (Phase 19)

### How Auto-Join Works

1. All players in the same Discord voice channel who launch the Activity share the same `instanceId`
2. The client sends `join-or-create` with the `instanceId` as the room key
3. First player creates the room; subsequent players join the existing room
4. If a game is already in progress, late joiners become spectators
5. When the game ends, all players (including spectators) return to the lobby

### Instance ID Behavior

- `instanceId` is stable when multiple players are in the Activity
- If a solo player pops out the Activity, `instanceId` may change (they get a new room)
- This is expected Discord SDK behavior (GitHub issue #202)

### Testing Discord Auto-Join Locally

1. Start the dev environment: `make dev-discord`
2. Note the cloudflared tunnel URL from the output
3. Update Discord Developer Portal URL mappings to point to the tunnel URL
4. Open Discord, join a voice channel
5. Launch the Activity — you should auto-join a lobby with no manual steps
6. Have a second account open the Activity in the same voice channel
7. They should appear in the same lobby automatically
```

**4. Verify cookie configuration in server:**
Check if `packages/server/src/routes/api.ts` (the `/api/token` endpoint) sets any cookies. If not, no changes needed — document that no cookies are currently used. If it does set cookies, add `SameSite=None; Partitioned; Secure` attributes.
  </action>
  <verify>
Run full verification suite:
```bash
cd /Users/stephendickson/Personal/shit-head
make type-check    # All packages pass
make test          # All tests pass (may need to skip known pre-existing failures)
make lint          # Zero lint errors
```
Verify DISCORD-SETUP.md exists with the new sections.
  </verify>
  <done>
    - DISCORD-SETUP.md has production deployment section with env vars and URL mapping config
    - DISCORD-SETUP.md has cookie configuration documentation
    - DISCORD-SETUP.md has Phase 19 auto-join explanation and testing instructions
    - cloudflared tunnel setup verified/updated
    - All type checks pass
    - All tests pass
    - Lint passes
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Discord Activity integration with:
- Instance ID auto-join (players in same voice channel share a room)
- Host migration (room survives host disconnect)
- Spectator mode (late joiners watch, then join next game)
- Auto-return to lobby after game over (no play-again button)
- Shithead marker (poo emoji next to previous loser)
- Discord avatars in lobby and game
- Safe area CSS for mobile Discord
- Updated deployment documentation
  </what-built>
  <how-to-verify>
1. **Web mode (standalone)** — verify nothing is broken:
   a. Open the app in a browser (`make dev` then visit localhost)
   b. Create a room, verify lobby shows normally
   c. Open a second browser tab, join the room
   d. Verify game plays through correctly
   e. When game ends, verify auto-return to lobby (no play-again button)
   f. Verify the loser has a poo emoji next to their name in the lobby

2. **Discord mode** — verify full flow:
   a. Start `make dev-discord`
   b. Update Discord URL mappings to cloudflared tunnel URL
   c. Open Discord, join a voice channel
   d. Launch the Activity
   e. Verify: auto-authentication -> auto-join lobby (no name entry, no room code)
   f. Verify: Discord avatar appears next to your name
   g. Have another Discord account open the Activity in the same voice channel
   h. Verify: they auto-join the same lobby
   i. Play a game, verify the loser gets the poo emoji
   j. Verify auto-return to lobby after game ends

3. **Host migration** (web or Discord):
   a. Start a 3-player game
   b. Have the host close their browser tab
   c. Verify: another player becomes host, game continues (room NOT destroyed)

4. **Spectator mode** (web or Discord):
   a. Start a 2-player game
   b. While game is in progress, have a 3rd player join
   c. Verify: 3rd player sees "Spectating - you'll join next game" message
   d. Verify: active players see spectator count indicator
   e. When game ends, verify spectator joins the lobby as a regular player

5. **Safe area** (Discord mobile):
   a. Open Activity on a mobile device with a notch (iPhone)
   b. Verify UI is not cut off by the notch or home indicator
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `make type-check` passes for all packages
- `make test` passes (excluding known pre-existing failures)
- `make lint` passes
- DISCORD-SETUP.md is complete and accurate
- Human verification of Discord Activity flow, web flow, host migration, spectator mode, and safe area
</verification>

<success_criteria>
- Full Discord Activity works: auto-join, avatars, game play, auto-return
- Web mode fully functional (no regressions)
- Host migration works on both platforms
- Spectator mode works on both platforms
- Safe area CSS prevents mobile cutoff
- Documentation complete
</success_criteria>

<output>
After completion, create `.planning/phases/19-discord-room-management/19-06-SUMMARY.md`
</output>
