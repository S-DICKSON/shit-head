---
phase: 24-playwright-responsive-e2e-testing
plan: 02
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - packages/e2e/tests/home.spec.ts
  - packages/e2e/tests/lobby.spec.ts
autonomous: true

must_haves:
  truths:
    - "Home screen renders heading, nickname input, create/join buttons at all 6 viewports"
    - "Home screen form fits within viewport without horizontal scroll at all viewports"
    - "Create room button becomes enabled after WebSocket connects"
    - "Lobby screen shows player name after creating a room"
    - "Lobby screen renders leave button and player count at all viewports"
    - "Room code is visible on lobby screen"
  artifacts:
    - path: "packages/e2e/tests/home.spec.ts"
      provides: "Home screen E2E tests"
      min_lines: 30
    - path: "packages/e2e/tests/lobby.spec.ts"
      provides: "Lobby screen E2E tests"
      min_lines: 30
  key_links:
    - from: "packages/e2e/tests/home.spec.ts"
      to: "packages/e2e/tests/pages/LandingPage.ts"
      via: "import LandingPage POM"
      pattern: "import.*LandingPage"
    - from: "packages/e2e/tests/lobby.spec.ts"
      to: "packages/e2e/tests/pages/LandingPage.ts"
      via: "import LandingPage for room creation flow"
      pattern: "import.*LandingPage"
    - from: "packages/e2e/tests/lobby.spec.ts"
      to: "packages/e2e/tests/pages/LobbyPage.ts"
      via: "import LobbyPage POM"
      pattern: "import.*LobbyPage"
---

<objective>
Write E2E tests for the home screen and lobby screen that run across all 6 viewport projects, using real WebSocket connections to the actual server.

Purpose: Validate that the landing page (create/join room) and lobby (player list, host controls) render correctly and function at all responsive breakpoints.
Output: home.spec.ts and lobby.spec.ts test files that pass across all 6 viewports.
</objective>

<execution_context>
@/Users/stephendickson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stephendickson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/24-playwright-responsive-e2e-testing/24-RESEARCH.md
@.planning/phases/24-playwright-responsive-e2e-testing/24-01-SUMMARY.md
@packages/client/src/components/Landing.vue
@packages/client/src/components/Lobby.vue
@packages/client/src/components/RoomCode.vue
@packages/client/src/router.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write home screen E2E tests</name>
  <files>packages/e2e/tests/home.spec.ts</files>
  <action>
Create `packages/e2e/tests/home.spec.ts` with the following tests using the LandingPage POM:

```typescript
import { test, expect } from '@playwright/test';
import { LandingPage } from './pages/LandingPage';
```

**Test: renders heading and form at all viewports**
- Navigate to landing page via POM `goto()`
- Assert heading "Karma" is visible
- Assert nickname input is visible
- Assert Create New Room button is visible
- Assert Join Room button is visible

**Test: form fits within viewport without horizontal scroll**
- Navigate to landing page
- Evaluate `document.documentElement.scrollWidth` and `document.documentElement.clientWidth`
- Assert scrollWidth <= clientWidth (no horizontal overflow)

**Test: create room button becomes enabled after WS connects**
- Navigate to landing page
- Fill nickname with "TestUser"
- Wait for Create New Room button to be enabled (timeout: 5000ms) — this confirms WS connection
- Assert button is enabled

**Test: create room button disabled without nickname**
- Navigate to landing page
- Wait for WS connection (use `waitForConnected()` from POM)
- Assert Create New Room button is disabled (no nickname entered)

**Test: join room button disabled without room code**
- Navigate to landing page
- Wait for WS connection
- Fill nickname "TestUser"
- Assert Join Room button is disabled (no room code entered)

**Test: nickname input accepts up to 20 characters**
- Navigate to landing page
- Fill nickname with 25-char string
- Assert input value is truncated to 20 chars (maxlength attribute)

All tests use the REAL WebSocket server (no mocking). The `webServer` config in playwright.config.ts handles starting the server and client automatically.

Important: The app uses hash-based routing. `page.goto('/')` navigates to the root which renders the Landing component at `/#/`. No special URL handling needed for home tests.
  </action>
  <verify>
    - `cd packages/e2e && bunx playwright test home.spec.ts --project=laptop-1280` passes
    - `cd packages/e2e && bunx playwright test home.spec.ts` passes across all 6 viewports
  </verify>
  <done>
    home.spec.ts passes across all 6 viewport projects, validating Landing screen renders and functions correctly at all breakpoints.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write lobby screen E2E tests</name>
  <files>packages/e2e/tests/lobby.spec.ts</files>
  <action>
Create `packages/e2e/tests/lobby.spec.ts` with the following tests. These tests use REAL WebSocket connections — they create actual rooms via the server.

```typescript
import { test, expect } from '@playwright/test';
import { LandingPage } from './pages/LandingPage';
import { LobbyPage } from './pages/LobbyPage';
```

**Test: create room navigates to lobby**
- Create LandingPage, navigate, wait for connected
- Create room with nickname "Player1"
- Wait for URL to match hash-based lobby route: `await page.waitForURL(/\/#\/room\//)` (hash router)
- Create LobbyPage, assert players heading is visible

**Test: lobby shows player name**
- Create room with nickname "LobbyTester"
- Wait for lobby navigation
- Assert text "LobbyTester" is visible on page

**Test: lobby shows room code**
- Create room with nickname "CodeCheck"
- Wait for lobby navigation
- Assert a 6-character alphanumeric code is visible (use regex locator or check the RoomCode component output)

**Test: leave button is visible**
- Create room with nickname "LeaveTest"
- Wait for lobby navigation
- Assert "Leave Room" text is visible

**Test: lobby fits within viewport**
- Create room and navigate to lobby
- Assert no horizontal scroll (scrollWidth <= clientWidth)

**Test: host sees start game button**
- Create room with nickname "HostTest"
- Wait for lobby navigation
- Assert "Waiting for players..." button text is visible (disabled because < minPlayers)

Important implementation notes:
- Each test creates a fresh room (server handles cleanup on WS close)
- Use `page.waitForURL(/\/#\/room\//)` for hash-based routing detection
- The lobby redirects to landing if no roomState exists, so tests MUST go through the create-room flow from Landing
- Wait for WS connection before interacting with buttons
- Room creation is fast (< 500ms) but add reasonable timeouts for navigation assertions
  </action>
  <verify>
    - `cd packages/e2e && bunx playwright test lobby.spec.ts --project=laptop-1280` passes
    - `cd packages/e2e && bunx playwright test lobby.spec.ts` passes across all 6 viewports
  </verify>
  <done>
    lobby.spec.ts passes across all 6 viewport projects, validating lobby screen renders player names, room code, host controls at all breakpoints.
  </done>
</task>

</tasks>

<verification>
- `cd packages/e2e && bunx playwright test home.spec.ts lobby.spec.ts` passes across all 6 viewports
- Tests use LandingPage and LobbyPage POMs (no raw CSS selectors)
- Tests use real WebSocket connections (no mocking)
- No horizontal scroll at any viewport
</verification>

<success_criteria>
- home.spec.ts has 5+ tests covering rendering, form validation, and WS connection
- lobby.spec.ts has 5+ tests covering room creation flow, player display, and layout
- All tests pass across all 6 viewport projects (6 x N tests total)
- Tests use POMs for locators
- Tests use real server (not mocked)
</success_criteria>

<output>
After completion, create `.planning/phases/24-playwright-responsive-e2e-testing/24-02-SUMMARY.md`
</output>
