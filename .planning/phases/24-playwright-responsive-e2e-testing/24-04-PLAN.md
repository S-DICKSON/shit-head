---
phase: 24-playwright-responsive-e2e-testing
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/e2e/playwright.config.ts
  - packages/e2e/tests/swap-phase.spec.ts
  - packages/e2e/tests/game.spec.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "All 6 Playwright projects use Chromium, so all tests run uniformly"
    - "WS-mocked tests in swap-phase.spec.ts and game.spec.ts run on all 6 viewports with no skips"
    - "Playwright UI mode shows all specs running (no skipped WebKit tests cluttering the view)"
    - "Mobile viewport dimensions (375x667, 390x844) are preserved with touch enabled"
  artifacts:
    - path: "packages/e2e/playwright.config.ts"
      provides: "All-Chromium project config with mobile viewports"
      contains: "hasTouch: true"
    - path: "packages/e2e/tests/swap-phase.spec.ts"
      provides: "Swap phase tests without browser skip guards"
    - path: "packages/e2e/tests/game.spec.ts"
      provides: "Game tests without browser skip guards"
  key_links:
    - from: "packages/e2e/playwright.config.ts"
      to: "mobile-375, mobile-390 projects"
      via: "viewport + hasTouch config (no devices[] spread)"
      pattern: "hasTouch.*true"
---

<objective>
Switch mobile-375 and mobile-390 Playwright projects from WebKit device presets to Chromium with custom mobile viewports. Remove the `test.skip(browserName !== 'chromium')` guards from WS-mocked test files.

Purpose: All 4 UAT gaps share the same root cause -- WebKit projects can't use routeWebSocket, so WS-mocked tests skip on mobile viewports. Switching to Chromium makes all 6 projects run all tests uniformly, fixing the Playwright UI confusion and making CI behavior match local.
Output: Updated playwright.config.ts, swap-phase.spec.ts, game.spec.ts
</objective>

<execution_context>
@/Users/stephendickson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stephendickson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-playwright-responsive-e2e-testing/24-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Switch mobile projects to Chromium with mobile viewports</name>
  <files>packages/e2e/playwright.config.ts</files>
  <action>
  Replace the mobile-375 and mobile-390 project configs. Remove the `...devices['iPhone SE']` and `...devices['iPhone 14']` spreads (these pull in WebKit as the browser). Instead, use explicit Chromium-compatible mobile viewport configs with touch enabled.

  Change mobile-375 from:
  ```
  {
    name: 'mobile-375',
    use: {
      ...devices['iPhone SE'],
      viewport: { width: 375, height: 667 },
    },
  }
  ```
  To:
  ```
  {
    name: 'mobile-375',
    use: {
      viewport: { width: 375, height: 667 },
      hasTouch: true,
      isMobile: true,
    },
  }
  ```

  Change mobile-390 from:
  ```
  {
    name: 'mobile-390',
    use: {
      ...devices['iPhone 14'],
      viewport: { width: 390, height: 844 },
    },
  }
  ```
  To:
  ```
  {
    name: 'mobile-390',
    use: {
      viewport: { width: 390, height: 844 },
      hasTouch: true,
      isMobile: true,
    },
  }
  ```

  Also remove the `devices` import from the top of the file since it is no longer used:
  Change `import { defineConfig, devices } from '@playwright/test';`
  To `import { defineConfig } from '@playwright/test';`

  Preserve all other projects (tablet-768, laptop-1280, desktop-1920, discord-iframe-460) and webServer config unchanged.
  </action>
  <verify>Run `cd /Users/stephendickson/Personal/shit-head && make lint` to confirm no lint errors. Visually confirm the config has no `devices` references.</verify>
  <done>mobile-375 and mobile-390 use Chromium (default) with custom viewport dimensions and touch/mobile flags. No WebKit device presets remain.</done>
</task>

<task type="auto">
  <name>Task 2: Remove Chromium-only skip guards from WS-mocked tests</name>
  <files>packages/e2e/tests/swap-phase.spec.ts, packages/e2e/tests/game.spec.ts</files>
  <action>
  In swap-phase.spec.ts, remove ALL lines matching:
  ```
  test.skip(browserName !== 'chromium', 'WS mocking requires Chromium');
  ```
  There are 6 occurrences (one per test in the describe block). Also remove the `browserName` destructured parameter from each test callback since it is no longer used. Change patterns like:
  ```
  test('renders swap phase UI', async ({ page, browserName }) => {
    test.skip(browserName !== 'chromium', 'WS mocking requires Chromium');
  ```
  To:
  ```
  test('renders swap phase UI', async ({ page }) => {
  ```

  Do the same in game.spec.ts -- there are 7 occurrences (one per test). Remove all `test.skip(browserName !== 'chromium'...)` lines and remove `browserName` from destructured params.

  Do NOT modify any other test logic, assertions, or helper functions.
  </action>
  <verify>
  Run `cd /Users/stephendickson/Personal/shit-head && make lint` to confirm no lint errors (unused vars would fail).
  Run `cd /Users/stephendickson/Personal/shit-head/packages/e2e && bunx playwright test --project=mobile-375 tests/swap-phase.spec.ts` to confirm mobile viewport now runs swap-phase tests (should pass, not skip).
  Run `cd /Users/stephendickson/Personal/shit-head/packages/e2e && bunx playwright test` to confirm all 150 tests pass with 0 skipped.
  </verify>
  <done>All WS-mocked tests run on all 6 viewport projects. Zero skipped tests. `make e2e` shows ~150 passing, 0 skipped.</done>
</task>

</tasks>

<verification>
1. `make lint` passes (no unused imports or variables)
2. `cd packages/e2e && bunx playwright test` shows all tests passing, 0 skipped
3. `cd packages/e2e && bunx playwright test --project=mobile-375 tests/game.spec.ts` runs (not skips) game tests on mobile viewport
4. `cd packages/e2e && bunx playwright test --project=mobile-390 tests/swap-phase.spec.ts` runs (not skips) swap-phase tests on mobile viewport
5. No `devices` import remains in playwright.config.ts
6. No `test.skip(browserName` remains in any test file
</verification>

<success_criteria>
- All 6 Playwright projects use Chromium (no WebKit)
- All tests run on all viewports -- 0 skipped tests
- Mobile viewports preserve 375x667 and 390x844 dimensions with touch enabled
- Playwright UI mode shows all specs running uniformly across all projects
</success_criteria>

<output>
After completion, create `.planning/phases/24-playwright-responsive-e2e-testing/24-04-SUMMARY.md`
</output>
