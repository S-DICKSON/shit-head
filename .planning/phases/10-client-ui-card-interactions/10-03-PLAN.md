---
phase: 10-client-ui-card-interactions
plan: 03
type: execute
wave: 2
depends_on: ["10-01", "10-02"]
files_modified:
  - packages/client/src/components/PlayingPhase.vue
  - packages/client/src/components/Game.vue
autonomous: true

must_haves:
  truths:
    - "PlayingPhase.vue assembles all sub-components into a complete game view"
    - "Game.vue renders PlayingPhase instead of placeholder during playing phase"
    - "UI works on mobile (vertical stack) and desktop (centered layout)"
    - "Cards animate smoothly when entering or leaving hand"
    - "Discard pile and draw pile are visible in the center game area"
    - "Opponents are displayed at the top of the screen"
  artifacts:
    - path: "packages/client/src/components/PlayingPhase.vue"
      provides: "Complete playing phase layout"
    - path: "packages/client/src/components/Game.vue"
      provides: "Updated to render PlayingPhase during playing phase"
  key_links:
    - from: "packages/client/src/components/PlayingPhase.vue"
      to: "packages/client/src/composables/usePlayingPhase.ts"
      via: "usePlayingPhase() composable"
      pattern: "usePlayingPhase\\(\\)"
    - from: "packages/client/src/components/PlayingPhase.vue"
      to: "packages/client/src/components/PlayerCards.vue"
      via: "component import and props"
      pattern: "PlayerCards"
    - from: "packages/client/src/components/PlayingPhase.vue"
      to: "packages/client/src/components/OpponentCards.vue"
      via: "component import and v-for"
      pattern: "OpponentCards"
    - from: "packages/client/src/components/PlayingPhase.vue"
      to: "packages/client/src/components/DiscardPile.vue"
      via: "component import and props"
      pattern: "DiscardPile"
    - from: "packages/client/src/components/PlayingPhase.vue"
      to: "packages/client/src/components/DrawPile.vue"
      via: "component import and props"
      pattern: "DrawPile"
    - from: "packages/client/src/components/Game.vue"
      to: "packages/client/src/components/PlayingPhase.vue"
      via: "conditional v-else-if rendering"
      pattern: "PlayingPhase"
---

<objective>
Assemble PlayingPhase.vue from all sub-components and wire it into Game.vue, replacing the placeholder "Game in progress..." text.

Purpose: This is the integration plan that brings together the composable and all sub-components into a working playing phase UI. After this plan, players see a complete responsive game interface during the playing phase.

Output: PlayingPhase.vue fully assembled, Game.vue updated to render it.
</objective>

<execution_context>
@/Users/stephendickson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stephendickson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-client-ui-card-interactions/10-RESEARCH.md
@.planning/phases/10-client-ui-card-interactions/10-01-SUMMARY.md
@.planning/phases/10-client-ui-card-interactions/10-02-SUMMARY.md

Key references:
@packages/client/src/components/Game.vue — Current file to update (replace playing phase placeholder)
@packages/client/src/composables/usePlayingPhase.ts — State/actions to use
@packages/client/src/components/PlayerCards.vue — Player card display
@packages/client/src/components/OpponentCards.vue — Opponent card display
@packages/client/src/components/DiscardPile.vue — Discard pile display
@packages/client/src/components/DrawPile.vue — Draw pile display
@packages/client/src/components/TurnTimer.vue — Existing turn timer component
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PlayingPhase.vue</name>
  <files>packages/client/src/components/PlayingPhase.vue</files>
  <action>
Create `PlayingPhase.vue` as the main playing phase layout component. This follows the same pattern as SwapPhase.vue — uses a composable for state and assembles sub-components.

**Script setup:**
```typescript
import { usePlayingPhase } from '../composables/usePlayingPhase';
import TurnTimer from './TurnTimer.vue';
import OpponentCards from './OpponentCards.vue';
import DrawPile from './DrawPile.vue';
import DiscardPile from './DiscardPile.vue';
import PlayerCards from './PlayerCards.vue';

const {
  gameView,
  roomState,
  playerId,
  turnTimeRemaining,
  turnTimerPlayerIndex,
  selectedHandIndices,
  selectedFaceUpIndex,
  isMyTurn,
  activeSource,
  hasSelection,
  toggleHandCard,
  selectFaceUpCard,
  selectFaceDownCard,
  playSelectedCards,
  pickupPile,
} = usePlayingPhase();
```

Add a helper to determine if an opponent is the current turn player:
```typescript
function isOpponentCurrentTurn(opponentPlayerId: string): boolean {
  if (!roomState.value || gameView.value?.currentPlayerIndex === undefined) return false;
  const opponentIndex = roomState.value.players.findIndex(p => p.id === opponentPlayerId);
  return opponentIndex === gameView.value.currentPlayerIndex;
}
```

**Template layout (mobile-first, vertical stack):**

```html
<div class="flex flex-col min-h-screen bg-green-900 text-white">
  <!-- Top bar: Turn Timer (centered) -->
  <div class="flex justify-center py-2">
    <TurnTimer :time-remaining="turnTimeRemaining" :total-time="45" />
  </div>

  <!-- Opponents row (scrollable horizontally if many) -->
  <div class="flex flex-wrap justify-center gap-2 sm:gap-4 px-2 mb-2">
    <OpponentCards
      v-for="opponent in gameView?.opponents"
      :key="opponent.playerId"
      :opponent="opponent"
      :is-current-turn="isOpponentCurrentTurn(opponent.playerId)"
    />
  </div>

  <!-- Spacer to push game area toward center -->
  <div class="flex-1" />

  <!-- Center game area: Draw Pile + Discard Pile side by side -->
  <div class="flex justify-center items-end gap-6 sm:gap-8 px-4 mb-4">
    <DrawPile :count="gameView?.drawPileCount ?? 0" />
    <DiscardPile :cards="gameView?.discardPile ?? []" />
  </div>

  <!-- Spacer -->
  <div class="flex-1" />

  <!-- Player's cards area (bottom of screen) -->
  <div class="px-2 pb-4">
    <PlayerCards
      :hand="gameView?.hand ?? []"
      :face-up="gameView?.faceUp ?? []"
      :face-down-count="gameView?.faceDownCount ?? 0"
      :selected-hand-indices="selectedHandIndices"
      :selected-face-up-index="selectedFaceUpIndex"
      :is-my-turn="isMyTurn"
      :active-source="activeSource"
      :has-selection="hasSelection"
      @toggle-hand-card="toggleHandCard"
      @select-face-up="selectFaceUpCard"
      @select-face-down="selectFaceDownCard"
      @play-cards="playSelectedCards"
      @pickup-pile="pickupPile"
    />
  </div>
</div>
```

This creates a natural card game layout:
- Timer at top
- Opponents just below timer
- Draw/discard piles in the middle
- Player's cards at the bottom (closest to player, like a real card table)
  </action>
  <verify>
Run `make type-check` — no TypeScript errors.
Run `make lint` — no ESLint errors.
  </verify>
  <done>PlayingPhase.vue assembles all sub-components with responsive layout: timer top, opponents below, draw/discard center, player cards bottom. Uses usePlayingPhase composable for all state and actions.</done>
</task>

<task type="auto">
  <name>Task 2: Wire PlayingPhase into Game.vue</name>
  <files>packages/client/src/components/Game.vue</files>
  <action>
Update Game.vue to replace the playing phase placeholder with the PlayingPhase component.

**Changes:**

1. Add import in script setup:
   `import PlayingPhase from './PlayingPhase.vue';`

2. Replace the playing phase placeholder div. Change this:
   ```html
   <div
     v-else-if="gameView.phase === 'playing'"
     class="min-h-screen flex flex-col items-center justify-center bg-green-900 text-white gap-4"
   >
     <TurnTimer
       :time-remaining="turnTimeRemaining"
       :total-time="45"
     />
     <div class="text-2xl font-bold">
       Game in progress...
     </div>
   </div>
   ```

   To this:
   ```html
   <PlayingPhase v-else-if="gameView.phase === 'playing'" />
   ```

3. Since PlayingPhase now handles its own TurnTimer, remove the TurnTimer import from Game.vue if it's no longer used elsewhere. Check: TurnTimer is NOT used in the finished or fallback phases, so it can be removed from Game.vue imports. However, keep it imported if the finished phase might use it later — actually, just remove the import since PlayingPhase handles it internally and the finished phase doesn't need it. Also remove `turnTimeRemaining` from the useGameSocket destructure in Game.vue since it's no longer used directly there.

**Verify the final Game.vue has:**
- SwapPhase for swapping/transitioning phases
- PlayingPhase for playing phase
- Finished phase placeholder (leave as-is for Phase 11)
- Loading fallback (leave as-is)
  </action>
  <verify>
Run `make type-check` — no TypeScript errors.
Run `make lint` — no ESLint errors.
Verify Game.vue renders PlayingPhase component during 'playing' phase.
  </verify>
  <done>Game.vue renders PlayingPhase during playing phase instead of placeholder. TurnTimer import removed from Game.vue (PlayingPhase handles it internally). All other phase renderings unchanged.</done>
</task>

</tasks>

<verification>
1. `make type-check` passes with no errors
2. `make lint` passes with no errors
3. `make test` passes (existing tests still work)
4. PlayingPhase.vue imports and renders all sub-components
5. Game.vue conditionally renders PlayingPhase during 'playing' phase
6. Layout is responsive: vertical stack works on mobile, centered on desktop
</verification>

<success_criteria>
- PlayingPhase.vue is a complete, responsive game view
- Game.vue renders PlayingPhase (not placeholder) during playing phase
- All sub-components receive correct props from usePlayingPhase composable
- Player can see opponents, draw pile, discard pile, and their own cards
- Turn timer visible at top of screen
- No TypeScript or ESLint errors
</success_criteria>

<output>
After completion, create `.planning/phases/10-client-ui-card-interactions/10-03-SUMMARY.md`
</output>
