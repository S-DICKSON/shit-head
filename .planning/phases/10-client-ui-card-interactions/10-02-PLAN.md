---
phase: 10-client-ui-card-interactions
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/client/src/components/DiscardPile.vue
  - packages/client/src/components/DrawPile.vue
  - packages/client/src/components/OpponentCards.vue
autonomous: true

must_haves:
  truths:
    - "Discard pile shows top 3 cards with stacked offset effect"
    - "Draw pile shows card count with card-back visual"
    - "Each opponent's face-up cards, hand count, and face-down count are visible"
    - "Current turn opponent is highlighted with a yellow border"
  artifacts:
    - path: "packages/client/src/components/DiscardPile.vue"
      provides: "Stacked discard pile visualization"
    - path: "packages/client/src/components/DrawPile.vue"
      provides: "Draw pile with card count badge"
    - path: "packages/client/src/components/OpponentCards.vue"
      provides: "Per-opponent card state display"
  key_links:
    - from: "packages/client/src/components/OpponentCards.vue"
      to: "packages/shared/src/types/game.ts"
      via: "OpponentView type for props"
      pattern: "OpponentView"
---

<objective>
Create the three game area sub-components: DiscardPile, DrawPile, and OpponentCards.

Purpose: These components visualize the shared game state (discard pile, draw pile, opponents) that all players see. They are presentational components with no local business logic.

Output: Three ready-to-use Vue components that accept props and render game area elements.
</objective>

<execution_context>
@/Users/stephendickson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stephendickson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-client-ui-card-interactions/10-RESEARCH.md

Key references:
@packages/client/src/components/SwapPhase.vue — Card rendering pattern, opponent display pattern
@packages/shared/src/types/game.ts — OpponentView type (playerId, nickname, faceDownCount, faceUp, handCount)
@packages/shared/src/types/card.ts — Card discriminated union
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DiscardPile and DrawPile components</name>
  <files>
    packages/client/src/components/DiscardPile.vue
    packages/client/src/components/DrawPile.vue
  </files>
  <action>
**DiscardPile.vue:**

Props:
- `cards: Card[]` — full discard pile array
- `burnAnimation?: boolean` — optional flag for burn visual (default false)

Computed:
- `visibleCards` — `cards.slice(-3)` to show only top 3 cards
- `isEmpty` — `cards.length === 0`
- `pileCount` — `cards.length`

Template:
- Container: `<div class="relative w-16 h-24 sm:w-20 sm:h-30">`
- Empty state: Show dashed border outline with "Pile" text when empty:
  `<div v-if="isEmpty" class="w-full h-full border-2 border-dashed border-green-600 rounded flex items-center justify-center text-green-500 text-xs">Pile</div>`
- Stacked cards: `<div v-else class="relative w-full h-full">`
  - v-for over visibleCards with index, each card positioned with:
    `:style="{ transform: \`translate(\${i * 3}px, \${i * 3}px)\`, zIndex: i }"`
    `class="absolute top-0 left-0 w-14 h-21 sm:w-16 sm:h-24 bg-white text-black rounded border border-gray-300 flex flex-col items-center justify-center text-xs sm:text-sm shadow-sm"`
  - Card content: rank + suit symbol (same suitSymbol helper as SwapPhase)
  - Key: `cardKey(card)` using suit-rank or joker-id
- Pile count badge: `<span class="absolute -top-2 -right-2 bg-green-700 text-white text-xs rounded-full w-6 h-6 flex items-center justify-center">{{ pileCount }}</span>` (only when pileCount > 0)
- Label below: `<div class="text-center text-xs text-green-300 mt-1">Discard</div>`

Include suitSymbol and cardKey helpers in script setup (same pattern as SwapPhase).

**DrawPile.vue:**

Props:
- `count: number` — number of cards remaining in draw pile

Template:
- Container: `<div class="relative">`
- When count > 0: Render a card-back visual:
  `<div class="w-14 h-21 sm:w-16 sm:h-24 bg-blue-800 rounded border-2 border-blue-600 flex items-center justify-center shadow-md">`
  Content: card back pattern — `<div class="w-10 h-16 sm:w-12 sm:h-18 border border-blue-400 rounded-sm"></div>` (inner rectangle decoration)
  Count badge: `<span class="absolute -top-2 -right-2 bg-blue-700 text-white text-xs rounded-full w-6 h-6 flex items-center justify-center">{{ count }}</span>`
- When count === 0: Show empty state:
  `<div class="w-14 h-21 sm:w-16 sm:h-24 border-2 border-dashed border-green-600 rounded flex items-center justify-center text-green-500 text-xs">Empty</div>`
- Label below: `<div class="text-center text-xs text-green-300 mt-1">Draw</div>`
  </action>
  <verify>
Run `make type-check` — no TypeScript errors.
Run `make lint` — no ESLint errors.
  </verify>
  <done>DiscardPile.vue renders stacked top-3 cards with count badge and empty state. DrawPile.vue renders card-back visual with count badge and empty state. Both mobile-responsive with sm: breakpoints.</done>
</task>

<task type="auto">
  <name>Task 2: Create OpponentCards component</name>
  <files>packages/client/src/components/OpponentCards.vue</files>
  <action>
Create `OpponentCards.vue` as a presentational component that renders one opponent's card state.

**Props (via defineProps):**
- `opponent: OpponentView` — from @shit-head/shared (playerId, nickname, faceDownCount, faceUp, handCount)
- `isCurrentTurn: boolean` — whether this opponent is the current turn player
- `isDisconnected?: boolean` — optional flag for disconnect visual (default false)

**Template:**
Container with current-turn highlight:
```
<div class="text-center p-2 rounded-lg transition-all"
     :class="isCurrentTurn ? 'bg-yellow-900/40 ring-2 ring-yellow-400' : ''">
```

1. **Nickname row:**
   `<div class="flex items-center justify-center gap-1 mb-1">`
   - Name with truncate: `<span class="text-sm font-medium truncate max-w-[100px]">{{ opponent.nickname }}</span>`
   - Turn indicator dot: `<span v-if="isCurrentTurn" class="w-2 h-2 bg-yellow-400 rounded-full animate-pulse"></span>`
   - Disconnected indicator: `<span v-if="isDisconnected" class="text-red-400 text-xs">(DC)</span>`

2. **Face-up cards** (visible to all):
   `<div class="flex gap-1 justify-center mb-1">`
   - v-for over opponent.faceUp with cardKey as key
   - Small card: `w-10 h-15 sm:w-12 sm:h-18 bg-white text-black rounded border border-gray-300 flex flex-col items-center justify-center text-[10px] sm:text-xs`
   - Card content: rank + suit symbol

3. **Hidden card indicators** (below face-up):
   `<div class="flex gap-1 justify-center text-[10px] text-green-300">`
   - Hand count: `<span v-if="opponent.handCount > 0">Hand: {{ opponent.handCount }}</span>`
   - Face-down count: `<span v-if="opponent.faceDownCount > 0">Down: {{ opponent.faceDownCount }}</span>`

Include suitSymbol and cardKey helpers in script setup. Import OpponentView type from @shit-head/shared.
  </action>
  <verify>
Run `make type-check` — no TypeScript errors.
Run `make lint` — no ESLint errors.
  </verify>
  <done>OpponentCards.vue renders per-opponent card state with nickname, face-up cards, hand/face-down counts, current-turn highlight with yellow ring, and disconnect indicator. Responsive with sm: breakpoints.</done>
</task>

</tasks>

<verification>
1. `make type-check` passes with no errors
2. `make lint` passes with no errors
3. DiscardPile.vue renders stacked top-3 cards with empty state and count badge
4. DrawPile.vue renders card-back with count and empty state
5. OpponentCards.vue renders opponent name, face-up cards, counts, and turn highlight
</verification>

<success_criteria>
- Three sub-components are ready for integration into PlayingPhase.vue
- Each component is purely presentational (props in, emits out, no side effects)
- Card rendering follows SwapPhase.vue patterns (suitSymbol, card styling)
- Current-turn opponent highlighted with yellow border
- Responsive card sizing (smaller on mobile, larger on desktop)
- TypeScript and ESLint pass cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/10-client-ui-card-interactions/10-02-SUMMARY.md`
</output>
