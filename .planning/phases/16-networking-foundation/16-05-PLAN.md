---
phase: 16-networking-foundation
plan: 05
type: execute
wave: 3
depends_on: ["16-01", "16-02", "16-03", "16-04"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "All automated tests pass (make test, make type-check, make lint)"
    - "ConnectionStatus overlay renders correctly in the browser"
    - "DISCORD-SETUP.md is complete and coherent"
    - "make dev still works for standalone web development"
  artifacts: []
  key_links: []
---

<objective>
Final verification of Phase 16 deliverables: run all checks, visually verify the connection status overlay, and confirm documentation completeness.

Purpose: Phase 16 is a risk-validation phase. Before moving to Phase 17 (Platform Abstraction), we need confidence that the proxy configuration, connection resilience, dev workflow, and tests are all solid.

Output: Verified phase completion, all checks green.
</objective>

<execution_context>
@/Users/stephendickson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stephendickson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-networking-foundation/16-01-SUMMARY.md
@.planning/phases/16-networking-foundation/16-02-SUMMARY.md
@.planning/phases/16-networking-foundation/16-03-SUMMARY.md
@.planning/phases/16-networking-foundation/16-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full automated verification suite</name>
  <files></files>
  <action>
Run the following verification commands and ensure all pass:

1. `make type-check` - TypeScript compilation for all packages (shared, server, client)
2. `make test` - All unit tests (server + client)
3. `make lint` - ESLint across all packages

If any command fails:
- Capture the error output with full context
- Fix the issue if it's a straightforward fix (import path, missing type, lint rule)
- Re-run the failed command to confirm the fix
- If the issue is complex or unclear, document it for the checkpoint

Also verify:
- `make help` shows `dev-discord` and `dev-discord-down` targets
- DISCORD-SETUP.md exists at project root and contains URL Mapping, Testing Checklist, and Production Deployment sections
  </action>
  <verify>`make type-check` exits 0, `make test` exits 0, `make lint` exits 0, `make help` shows dev-discord.</verify>
  <done>All automated checks pass: type-check, test, lint. Dev-discord target visible in help. DISCORD-SETUP.md complete.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Phase 16 Networking Foundation deliverables:
1. Vite proxy config with /.proxy path rules for Discord Activity mode
2. Client WebSocket URL resolution that detects Discord context
3. Server origin validation allowing *.discordsays.com
4. ConnectionStatus overlay (reconnecting spinner + failed state with retry button)
5. Enhanced reconnection with exponential backoff (1s->2s->4s...30s, 10 retries, jitter)
6. Discord dev workflow (make dev-discord with cloudflared tunnel)
7. DISCORD-SETUP.md with URL mappings, setup guide, testing checklist
8. Automated tests for URL resolution and origin validation
9. Experimental cloudflared CI workflow (manual trigger)
  </what-built>
  <how-to-verify>
1. Run `make dev` and confirm the existing standalone web app works normally (create room, join room)
2. Open the browser and verify NO connection overlay appears when connected normally
3. Stop the server container (`docker compose stop server`) and verify the "Reconnecting..." overlay appears with spinner
4. Wait ~30 seconds and verify the overlay changes to "Connection Failed" with a "Retry Connection" button
5. Restart the server (`docker compose start server`) and click "Retry Connection" — verify overlay disappears
6. Check DISCORD-SETUP.md in the project root — scan for completeness (URL mapping table, setup steps, testing checklist)
7. (Optional) If you have a Discord Application configured: run `make dev-discord` and test through the tunnel
  </how-to-verify>
  <resume-signal>Type "approved" if the connection overlay works and documentation looks good, or describe any issues.</resume-signal>
</task>

</tasks>

<verification>
1. All automated checks pass (type-check, test, lint)
2. Connection overlay appears on disconnect and disappears on reconnect
3. Retry button works after connection failure
4. DISCORD-SETUP.md is complete and discoverable
5. Standalone web mode works unchanged
</verification>

<success_criteria>
- Zero test failures, zero type errors, zero lint errors
- Connection resilience UI verified visually
- Human confirms phase deliverables are solid
- Ready to proceed to Phase 17 (Platform Abstraction)
</success_criteria>

<output>
After completion, create `.planning/phases/16-networking-foundation/16-05-SUMMARY.md`
</output>
