---
phase: 21-card-playability-highlights
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/client/src/game/cardRules.ts
  - packages/client/src/composables/usePlayingPhase.ts
  - packages/client/src/components/PlayingPhase.vue
  - packages/client/src/components/PlayerCards.vue
autonomous: true

must_haves:
  truths:
    - "Cards in hand show green glow when playable on current pile during player's turn"
    - "Cards in hand show gray/dimmed state when not playable during player's turn"
    - "No playability highlights appear when it is not the player's turn"
    - "Highlights correctly reflect Shithead special rules (2s always playable, 8s transparent, 7-constraint, 10s always playable, jokers always playable)"
    - "First-turn cards use forcedCardIndices instead of canPlayOnPile"
    - "Face-up cards show playability highlights only when activeSource is face-up"
    - "Mobile grouped view shows playability ring on playable card groups"
  artifacts:
    - path: "packages/client/src/game/cardRules.ts"
      provides: "Client-side canPlayOnPile, getEffectiveTopCard, getRankValue pure functions"
      exports: ["canPlayOnPile", "getEffectiveTopCard", "getRankValue"]
    - path: "packages/client/src/composables/usePlayingPhase.ts"
      provides: "playableHandIndices and playableFaceUpIndices computed properties"
      contains: "playableHandIndices"
    - path: "packages/client/src/components/PlayerCards.vue"
      provides: "Visual playability CSS classes on card buttons and grouped view"
      contains: "playableHandIndices"
  key_links:
    - from: "packages/client/src/composables/usePlayingPhase.ts"
      to: "packages/client/src/game/cardRules.ts"
      via: "import canPlayOnPile"
      pattern: "import.*canPlayOnPile.*cardRules"
    - from: "packages/client/src/components/PlayingPhase.vue"
      to: "packages/client/src/composables/usePlayingPhase.ts"
      via: "destructured playableHandIndices and playableFaceUpIndices from usePlayingPhase()"
      pattern: "playableHandIndices.*playableFaceUpIndices"
    - from: "packages/client/src/components/PlayingPhase.vue"
      to: "packages/client/src/components/PlayerCards.vue"
      via: "props :playable-hand-indices and :playable-face-up-indices"
      pattern: ":playable-hand-indices"
    - from: "packages/client/src/components/PlayerCards.vue"
      to: "CSS class bindings"
      via: "conditional Tailwind classes using playableHandIndices.has(i)"
      pattern: "playableHandIndices\\.has\\(i\\)"
---

<objective>
Add visual playability indicators to hand and face-up cards so players instantly see which cards they can legally play on the current discard pile.

Purpose: Players currently have to mentally evaluate each card against pile rules (including special cards like 2s, 8s transparency, 7-constraint). Visual highlights eliminate guesswork, especially on mobile where hover tooltips don't exist.

Output: Green glow on playable cards, gray/dimmed state on unplayable cards, working across desktop card buttons and mobile grouped view.
</objective>

<execution_context>
@/Users/stephendickson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stephendickson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/21-card-playability-highlights/21-RESEARCH.md
@packages/server/src/game/CardRules.ts
@packages/server/src/game/CardComparison.ts
@packages/client/src/composables/usePlayingPhase.ts
@packages/client/src/components/PlayerCards.vue
@packages/client/src/components/PlayingPhase.vue
@packages/shared/src/types/game.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create client-side card rules and add playability computed properties</name>
  <files>
    packages/client/src/game/cardRules.ts
    packages/client/src/composables/usePlayingPhase.ts
    packages/client/src/components/PlayingPhase.vue
  </files>
  <action>
1. Create `packages/client/src/game/cardRules.ts` with client-side copies of three pure functions from the server. These are direct copies (not imports from server) to avoid cross-package dependency on server code:

   - `getRankValue(card: Card): number` — Copy from `packages/server/src/game/CardComparison.ts`. Standard cards map to numeric values (2=-1, 3=0, ..., A=11), jokers=999. Include the RANK_ORDER array and RANK_MAP.
   - `getEffectiveTopCard(discardPile: Card[]): Card | null` — Copy from `packages/server/src/game/CardRules.ts`. Walks backwards skipping 8s (transparent).
   - `canPlayOnPile(playedCard: Card, discardPile: Card[]): boolean` — Copy from `packages/server/src/game/CardRules.ts`. Handles empty pile, special cards (2/8/10 always playable), 8-transparency, 7-constraint (must play <=7), normal ordering (>=).

   Import `Card` and `Rank` types from `@shit-head/shared`. Add a comment at top: `// Client-side copy of server CardRules + CardComparison pure functions. Keep in sync with packages/server/src/game/`.

2. In `packages/client/src/composables/usePlayingPhase.ts`, add two new computed properties:

   **playableHandIndices** — `computed<Set<number>>`:
   - Return empty Set if `!gameView.value` or `!isMyTurn.value`
   - On first turn (`isFirstTurn.value === true`): return `forcedCardIndices.value` directly (first turn uses forced logic, not canPlayOnPile)
   - Normal turn: iterate `gameView.value.hand`, call `canPlayOnPile(card, gameView.value.discardPile)` for each, collect playable indices into a Set

   **playableFaceUpIndices** — `computed<Set<number>>`:
   - Return empty Set if `!gameView.value` or `!isMyTurn.value` or `activeSource.value !== 'face-up'`
   - Iterate `gameView.value.faceUp`, call `canPlayOnPile(card, gameView.value.discardPile)` for each, collect playable indices into a Set

   Import `canPlayOnPile` from `../game/cardRules`. Add both to the return object of `usePlayingPhase()`.

3. In `packages/client/src/components/PlayingPhase.vue`:
   - Destructure `playableHandIndices` and `playableFaceUpIndices` from `usePlayingPhase()` (add to existing destructuring on ~line 17-32)
   - Pass as new props to `<PlayerCards>`: `:playable-hand-indices="playableHandIndices"` and `:playable-face-up-indices="playableFaceUpIndices"`
  </action>
  <verify>
Run `cd /Users/stephendickson/Personal/shit-head && make type-check` to verify no TypeScript errors across all packages. The new file must type-check cleanly and the modified files must still type-check.
  </verify>
  <done>
Client-side cardRules.ts exists with canPlayOnPile/getEffectiveTopCard/getRankValue. usePlayingPhase returns playableHandIndices and playableFaceUpIndices computed Sets. PlayingPhase.vue passes both as props to PlayerCards. Type-check passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add visual playability CSS to PlayerCards desktop and mobile views</name>
  <files>
    packages/client/src/components/PlayerCards.vue
  </files>
  <action>
1. **Add new props** to `defineProps` in PlayerCards.vue:
   ```typescript
   playableHandIndices: Set<number>;    // indices of hand cards playable on pile
   playableFaceUpIndices: Set<number>;  // indices of face-up cards playable on pile
   ```

2. **Desktop hand card buttons** (the `<TransitionGroup>` branch, `v-else` after `shouldShowGrouped`):
   Update the `:class` binding on the `<button>` for each hand card. Current logic:
   - selected -> yellow ring
   - not selected -> border-gray-300
   - inactive source -> opacity-40

   New logic (order matters -- selected takes priority over playable):
   ```
   :class="[
     selectedHandIndices.has(i)
       ? 'ring-2 ring-yellow-400 -translate-y-2 border-yellow-400 shadow-lg'
       : playableHandIndices.size > 0 && playableHandIndices.has(i)
         ? 'border-green-400 shadow-md shadow-green-400/40'
         : playableHandIndices.size > 0
           ? 'border-gray-400 opacity-50'
           : 'border-gray-300',
     activeSource !== 'hand'
       ? 'opacity-40 cursor-not-allowed'
       : 'cursor-pointer hover:border-gray-400'
   ]"
   ```
   Key behaviors:
   - `playableHandIndices.size > 0` is the guard: when size is 0 (not my turn, or no highlights computed), ALL cards fall through to `border-gray-300` (no highlights shown -- preserves existing look).
   - When size > 0 and card IS playable: green border + green shadow glow.
   - When size > 0 and card is NOT playable: gray border + opacity-50 (dimmed).
   - Selected state (yellow ring) always overrides playability state.
   - `activeSource !== 'hand'` opacity-40 still overrides all (face-up/face-down phase).

3. **Face-up card buttons** (the table cards section):
   Update the `:class` binding on face-up card `<button>`. Current:
   - selected -> yellow ring
   - not selected -> border-gray-300
   - inactive source -> opacity-40

   New logic:
   ```
   :class="[
     selectedFaceUpIndex === (i - 1)
       ? 'ring-2 ring-yellow-400 -translate-y-2 border-yellow-400 shadow-lg'
       : playableFaceUpIndices.size > 0 && playableFaceUpIndices.has(i - 1)
         ? 'border-green-400 shadow-md shadow-green-400/40'
         : playableFaceUpIndices.size > 0
           ? 'border-gray-400 opacity-50'
           : 'border-gray-300',
     activeSource !== 'face-up'
       ? 'opacity-40 cursor-not-allowed'
       : 'cursor-pointer hover:border-gray-400'
   ]"
   ```
   Same pattern as hand cards. Only shows highlights when activeSource is face-up (since playableFaceUpIndices is empty Set otherwise).

4. **Mobile grouped view** (the `v-if="shouldShowGrouped"` branch):
   Add a helper function in `<script setup>`:
   ```typescript
   function isGroupPlayable(groupRank: string): boolean {
     if (props.playableHandIndices.size === 0) return false;
     // Check if any card index in this group is in the playable set
     return props.hand.some((card, i) => {
       const rank = card.kind === 'standard' ? card.rank : 'joker';
       return rank === groupRank && props.playableHandIndices.has(i);
     });
   }
   ```

   Update the grouped card container `<div>` class binding. Current: `class="bg-gray-800/50 rounded-lg p-2 flex items-center gap-3"` (no dynamic class).
   New:
   ```
   class="rounded-lg p-2 flex items-center gap-3 transition-all"
   :class="[
     isGroupPlayable(group.rank)
       ? 'bg-gray-800/50 ring-1 ring-green-400 shadow-sm shadow-green-400/30'
       : playableHandIndices.size > 0
         ? 'bg-gray-800/50 opacity-50'
         : 'bg-gray-800/50'
   ]"
   ```

   Also update the sample card visual inside the group. Currently uses `border-gray-300`. Update:
   ```
   :class="[
     isGroupPlayable(group.rank)
       ? 'border-green-400'
       : 'border-gray-300'
   ]"
   ```

5. **Face-down cards**: No changes. Face-down cards are played blind and should never show playability highlights. Leave the face-down button CSS exactly as-is.

6. Run `make lint` and fix any linting issues (unused imports, formatting). Run `make lint-fix` if needed.
  </action>
  <verify>
Run `cd /Users/stephendickson/Personal/shit-head && make type-check && make lint` to verify TypeScript and linting both pass. Visually verify the class binding logic is correct by reading the template.
  </verify>
  <done>
PlayerCards.vue accepts playableHandIndices and playableFaceUpIndices props. Desktop hand cards show green glow when playable, gray/dimmed when not, and no highlights when not player's turn. Face-up cards show same pattern when activeSource is face-up. Mobile grouped view shows green ring on playable groups and dims unplayable groups. Face-down cards unchanged. Type-check and lint pass.
  </done>
</task>

</tasks>

<verification>
1. `make type-check` passes -- all TypeScript across packages compiles cleanly
2. `make lint` passes -- no linting violations
3. `make test` passes -- existing tests unbroken (no test files modified)
4. Manual review: `packages/client/src/game/cardRules.ts` exports match server logic exactly
5. Manual review: playableHandIndices returns empty Set when not player's turn
6. Manual review: first-turn logic uses forcedCardIndices, not canPlayOnPile
7. Manual review: playableFaceUpIndices returns empty Set when activeSource !== 'face-up'
8. Manual review: mobile grouped view has isGroupPlayable helper and ring-green-400 class
</verification>

<success_criteria>
- Green glow (border-green-400 shadow-md shadow-green-400/40) appears on playable hand cards during player's turn
- Gray/dimmed state (border-gray-400 opacity-50) appears on unplayable hand cards during player's turn
- No highlights appear during opponent's turn (all cards show default border-gray-300)
- First turn shows only forced cards as playable (uses forcedCardIndices)
- Face-up cards show playability only when activeSource is face-up
- Mobile grouped view shows ring-1 ring-green-400 on playable groups
- Face-down cards have zero visual changes
- make type-check, make lint, and make test all pass
</success_criteria>

<output>
After completion, create `.planning/phases/21-card-playability-highlights/21-01-SUMMARY.md`
</output>
