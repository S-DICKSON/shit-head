---
phase: 02-websocket-infrastructure-room-management
plan: 05
task: 3
total_tasks: 3
status: blocked
last_updated: 2026-02-07T17:50:00Z
---

<current_state>
Phase 2 execution: 4 of 5 plans complete. Plan 02-05 has 2 auto tasks committed but is paused at checkpoint (Task 3: human-verify) because the WebSocket connection from browser to server is not working properly.

The blocker is a WebSocket connectivity issue that must be fixed before the checkpoint can be verified.
</current_state>

<completed_work>
## This Session

**Wave 1:**
- 02-01: Shared message protocol, room types, Zod schemas (dce2dfc, ac1ffd8)

**Wave 2 (parallel):**
- 02-02: Server room management engine via TDD — 31 tests, Room + RoomManager classes (fdeba38, e5fc388, 0d56d52)
- 02-03: Client WebSocket composable, Vue Router, Landing page (82e82be, 400c3a8, a87db62)

**Wave 3 (parallel):**
- 02-04: Server WebSocket handlers + index.ts integration — Zod validation, pub/sub broadcasting, 14 handler tests (a475ac9, 1a7e3d6, 624d4a0)
- 02-05 Tasks 1-2: RoomCode component + Lobby page (1074d3e, 57a439b) — committed but SUMMARY not yet created

**Orchestrator fixes:**
- 9f821ee: Fixed Docker Compose proxy ECONNREFUSED (Vite proxy targeting localhost instead of Docker service name)
</completed_work>

<remaining_work>
## Immediate: Fix WebSocket connection bug

The browser shows "Connecting to server..." on the Landing page. The server logs show rapid connect/disconnect cycles. Root cause analysis so far:

1. **Docker proxy fixed** (9f821ee) — Vite proxy now uses `VITE_SERVER_URL` env var for Docker networking
2. **HMR path conflict addressed** — Changed Vite HMR path to `/__hmr` to avoid `/ws` proxy collision
3. **Pong handling fixed** — Added `pong` filter in composable's watch to skip non-JSON heartbeat responses

Despite these fixes, the connection still cycles. The issue may be:
- The VueUse `useWebSocket` heartbeat config (`message: 'ping'`) vs Bun's WebSocket ping/pong protocol-level handling
- The auto-reconnect loop creating new connections before old ones fully close
- A remaining HMR/proxy conflict where Vite still intercepts `/ws`

**Uncommitted files:**
- `packages/client/vite.config.ts` — HMR path change + VITE_SERVER_URL env var
- `packages/client/src/composables/useGameSocket.ts` — pong filter in watch
- `docker-compose.yml` — already committed in 9f821ee

## After fix: Complete 02-05

- Task 3 (checkpoint:human-verify): Verify full create/join/lobby flow in browser
- Create 02-05-SUMMARY.md
- Update STATE.md

## After 02-05: Phase completion steps

- Check for uncommitted orchestrator corrections
- Run gsd-verifier on phase goal
- Update ROADMAP.md (mark Phase 2 complete)
- Update REQUIREMENTS.md (mark ROOM-01..ROOM-04, MULT-01, MULT-02 as Complete)
- Commit phase completion docs
- Offer next: Phase 3 (Deck & Dealing System)
</remaining_work>

<decisions_made>
- Vite proxy uses `VITE_SERVER_URL` env var (defaults to localhost:3000, Docker sets to http://server:3000)
- HMR WebSocket path moved to `/__hmr` to avoid collision with game `/ws` proxy
- Composable filters `pong` text messages before JSON parsing
- Host leaving destroys room entirely (no host transfer) — per CONTEXT.md
- Custom nanoid alphabet excludes confusable chars (0/O, 1/I/L, 5/S)
- Schemas are single source of truth, types inferred via z.infer
</decisions_made>

<blockers>
**WebSocket connection cycling** — Browser can't maintain stable connection to server.

Debugging approach that hasn't been tried yet:
1. Test locally (not Docker): `bun run dev` for both server and client separately — see if the issue is Docker-specific or fundamental
2. Check browser DevTools Network tab for WS frames to see what's actually happening
3. Try disabling VueUse heartbeat entirely to see if plain WebSocket works
4. Consider if the Vite WS proxy and the game WS connection are interfering
5. Try a different WebSocket path (e.g., `/game-ws` instead of `/ws`) to completely avoid Vite's WS handling
</blockers>

<context>
The execute-phase orchestrator was running Phase 2 with wave-based parallel execution. Waves 1-3 all completed their code tasks. The only remaining work is:
1. Fix the WS connection bug (blocking the checkpoint)
2. Complete the checkpoint verification
3. Run phase completion steps (verify, update roadmap, offer next)

The most promising debugging avenue is changing the WebSocket path from `/ws` to something like `/game-ws` that Vite won't try to intercept. Vite reserves `/ws` for its own HMR in some configurations, and even with `hmr.path` set, there may be residual interference.

Model profile: balanced (executor=sonnet, verifier=sonnet)
Config: mode=yolo, parallelization=true, commit_docs=true
</context>

<next_action>
1. First, fix the WebSocket connection bug. Most likely fix: change WS endpoint from `/ws` to `/game-ws` in:
   - `packages/server/src/index.ts` (line 27: `url.pathname === '/ws'`)
   - `packages/client/vite.config.ts` (proxy config: `'/ws'` -> `'/game-ws'`)
   - `packages/client/src/composables/useGameSocket.ts` (line 13: URL path `/ws`)
2. Test locally first (`bun run src/index.ts` + `bun run dev`), then Docker
3. Once connection works, run `/gsd:execute-phase 2` to resume — it will detect completed SUMMARYs and pick up from 02-05 checkpoint
</next_action>
