---
phase: 02-websocket-infrastructure-room-management
plan: 05
type: execute
wave: 3
depends_on: ["02-03"]
files_modified:
  - packages/client/src/components/Lobby.vue
  - packages/client/src/components/RoomCode.vue
autonomous: false

must_haves:
  truths:
    - "Lobby displays room code prominently and a shareable URL"
    - "Lobby shows vertical list of player nicknames with host marked by icon"
    - "Start button is disabled until 2+ players, shows hint 'Waiting for players...'"
    - "Host clicking start triggers 3-5 second countdown visible to all"
    - "Players leaving the lobby disappear from the list"
  artifacts:
    - path: "packages/client/src/components/Lobby.vue"
      provides: "Lobby page with player list and host controls"
    - path: "packages/client/src/components/RoomCode.vue"
      provides: "Room code display with copy and share link"
  key_links:
    - from: "packages/client/src/components/Lobby.vue"
      to: "packages/client/src/composables/useGameSocket.ts"
      via: "uses useGameSocket for room state and actions"
      pattern: "useGameSocket"
    - from: "packages/client/src/components/Lobby.vue"
      to: "packages/client/src/components/RoomCode.vue"
      via: "includes RoomCode component"
      pattern: "RoomCode"
---

<objective>
Build the Lobby and RoomCode components that display room state, player list, host controls, and the shareable room code/link. This completes the client UI for the room management flow.

Purpose: The Lobby is where players wait together before the game starts. It must show who's in the room, let the host start the game, and provide an easy way to share the room code with friends. This is the "waiting room" experience.

Output: Lobby.vue and RoomCode.vue components implementing all locked decisions from CONTEXT.md.
</objective>

<execution_context>
@/Users/stephendickson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stephendickson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-websocket-infrastructure-room-management/02-CONTEXT.md
@.planning/phases/02-websocket-infrastructure-room-management/02-RESEARCH.md
@.planning/phases/02-websocket-infrastructure-room-management/02-03-SUMMARY.md
@packages/client/src/composables/useGameSocket.ts
@packages/client/src/router.ts
@packages/client/src/components/Landing.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build RoomCode component</name>
  <files>
    packages/client/src/components/RoomCode.vue
  </files>
  <action>
    Create `packages/client/src/components/RoomCode.vue`:

    **Props:** `code: string` (the 6-character room code)

    **Display (per CONTEXT.md: "show the room code prominently AND a shareable URL"):**
    - Large, bold room code displayed with letter-spacing for readability (e.g., "A B C D E F")
    - Copy code button (copies just the code to clipboard)
    - Shareable link displayed below: `${window.location.origin}/#/room/${code}` (or construct from current URL)
    - Copy link button (copies full URL to clipboard)
    - Use navigator.clipboard.writeText() for copy. Show brief "Copied!" feedback (transition from "Copy" to "Copied!" text for 2 seconds)

    **Styling:**
    - Room code: monospace font, large text (text-3xl or text-4xl), tracking-widest
    - Contained in a subtle bordered section or card
    - Copy buttons: small, inline, icon or text button
    - Share URL: smaller text, truncated if needed, with copy button
    - Label: "Room Code" above the code, "Share Link" above the URL
  </action>
  <verify>
    `cd packages/client && bunx vue-tsc --noEmit` passes. Component renders with a room code prop.
  </verify>
  <done>
    RoomCode component displays room code prominently with copy button and shareable URL with copy button.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build Lobby page</name>
  <files>
    packages/client/src/components/Lobby.vue
  </files>
  <action>
    Create `packages/client/src/components/Lobby.vue`:

    **Layout (centered on green background from App.vue):**
    - White card container (like Landing page style)
    - RoomCode component at the top
    - Divider
    - "Players" section header with count (e.g., "Players (2/4)")
    - Vertical list of player nicknames
    - Start button (host only) or "Waiting for host to start..." (non-host)
    - Countdown overlay when game is starting

    **Player List (per CONTEXT.md: "Simple vertical list of nicknames, host marked with a crown/star icon"):**
    - Each player row: nickname text + crown/star icon if host
    - Use a simple star or crown Unicode character or SVG icon (keep it simple, no external icon library)
    - Host's row could be slightly styled differently (bold or highlighted)
    - Show "(You)" next to the current player's name
    - Players listed in join order

    **Host Controls (per CONTEXT.md):**
    - "Start Game" button — only visible to host player
    - Button disabled when < 2 players, with text "Waiting for players..." (per CONTEXT.md locked decision)
    - When enabled (2+ players): green button, "Start Game"
    - On click: send `{ type: 'start-game' }` message

    **Non-Host View:**
    - Instead of start button, show "Waiting for host to start..." text

    **Countdown (per CONTEXT.md: "Short countdown (3-5 seconds) after host clicks start"):**
    - When `game-starting` message received: show countdown overlay
    - Large number counting down (3... 2... 1...)
    - Overlay the lobby content with a semi-transparent backdrop
    - When `game-started` received: for now, show "Game started!" placeholder (Phase 3 handles actual dealing)

    **Behavior:**
    - Import and use `useGameSocket` composable
    - Read roomState from composable (reactive)
    - Determine if current player is host: compare playerId with roomState.hostId
    - Handle `room-updated` and `player-left` messages: composable should reactively update roomState
    - Handle `game-starting`: show countdown
    - Handle `game-started`: show placeholder or navigate to game view (Phase 3)
    - On component mount: check if roomState exists. If not (e.g., direct URL navigation), redirect to landing page with the code pre-filled (they need to enter a nickname first)
    - "Leave Room" link/button: sends leave-room message, navigates back to landing

    **Styling:**
    - Consistent with Landing page (white card, rounded, shadow)
    - Player list: divide-y for separators, py-3 per row
    - Start button: full width, large, green bg, bold text
    - Disabled button: gray, cursor-not-allowed
    - Countdown: fixed overlay, large text (text-6xl), dark semi-transparent bg
    - Responsive: works on mobile and desktop
  </action>
  <verify>
    `cd packages/client && bunx vue-tsc --noEmit` passes. `cd packages/client && bun run build` succeeds. Component renders at `/room/:code` route.
  </verify>
  <done>
    Lobby page shows room code (via RoomCode component), player list with host icon, start button for host (disabled until 2+ players), countdown overlay on game start. Non-host sees waiting message. Leave room navigates back to landing.
  </done>
</task>

<task type="checkpoint:human-verify" gate="soft">
  <what-built>Complete room creation and joining flow: Landing page (nickname + create/join) -> Lobby (player list, room code, host controls). Server handles WebSocket messages for room lifecycle.</what-built>
  <how-to-verify>
    1. Start server: `cd packages/server && bun run dev`
    2. Start client: `cd packages/client && bun run dev`
    3. Open browser to http://localhost:5173

    **Test create flow:**
    4. Enter a nickname (e.g., "Alice"), click "Create Room"
    5. Verify: navigated to lobby, room code displayed, "Alice" in player list with host icon, start button disabled ("Waiting for players...")

    **Test join flow:**
    6. Open second browser tab to http://localhost:5173
    7. Enter nickname (e.g., "Bob"), paste room code, click "Join"
    8. Verify: both tabs show both players in the list, Alice's tab now has enabled "Start Game" button

    **Test start flow:**
    9. Click "Start Game" on Alice's tab
    10. Verify: countdown (3-2-1) appears on both tabs

    **Test leave flow:**
    11. Close Bob's tab or click leave
    12. Verify: Bob disappears from Alice's player list

    **Test error handling:**
    13. Try joining with invalid code — error message shown
    14. Try joining a full room (create room, join 3 more players) — "Room is full" error

    **Test share link:**
    15. Copy the share link from the lobby
    16. Open in new tab — should pre-fill or navigate to join flow
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
1. `cd packages/client && bunx vue-tsc --noEmit` passes
2. `cd packages/client && bun run build` succeeds
3. Full flow works: create room -> see lobby -> share code -> join from another tab -> see players -> start countdown
4. Error states work: invalid code, full room
5. Leave/disconnect removes player from list
</verification>

<success_criteria>
- Lobby displays room code prominently with shareable URL
- Player list shows all members with host crown/star icon
- Host sees start button (disabled until 2+ players, enabled after)
- Non-host sees "waiting for host" message
- Countdown appears on all clients when host starts
- Players disappear from list when they leave
- Client builds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-websocket-infrastructure-room-management/02-05-SUMMARY.md`
</output>
