---
phase: 15-mobile-ux-improvements
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - packages/client/src/composables/useSoundEffects.ts
  - packages/client/src/composables/useCardGrouping.ts
  - packages/client/src/components/TurnBanner.vue
  - packages/client/src/components/PlayerCards.vue
  - packages/client/src/components/PlayingPhase.vue
autonomous: true

must_haves:
  truths:
    - "Current player's turn triggers a sound notification when the turn banner appears"
    - "Hand cards are grouped by rank on mobile with +/- quantity selectors"
    - "Play button remains accessible (sticky at bottom) even with many cards"
  artifacts:
    - path: "packages/client/src/composables/useSoundEffects.ts"
      provides: "Sound notification composable with mobile autoplay-safe initialization"
      min_lines: 20
    - path: "packages/client/src/composables/useCardGrouping.ts"
      provides: "Card grouping composable that groups hand cards by rank"
      min_lines: 25
    - path: "packages/client/src/components/TurnBanner.vue"
      provides: "Turn banner with sound notification on appearance"
      contains: "useSoundEffects"
    - path: "packages/client/src/components/PlayerCards.vue"
      provides: "Mobile card grouping UI with +/- selectors and sticky play button"
      contains: "useCardGrouping"
  key_links:
    - from: "packages/client/src/components/TurnBanner.vue"
      to: "packages/client/src/composables/useSoundEffects.ts"
      via: "watch on visible prop triggers playTurnNotification"
      pattern: "playTurnNotification"
    - from: "packages/client/src/components/PlayerCards.vue"
      to: "packages/client/src/composables/useCardGrouping.ts"
      via: "import for grouping hand cards on mobile"
      pattern: "useCardGrouping"
---

<objective>
Implement turn sound notifications and mobile card grouping: (1) play a notification sound when it becomes the player's turn, (2) group hand cards by rank with +/- quantity selectors on mobile screens, (3) ensure the play button stays accessible with sticky positioning.

Purpose: Sound alerts ensure players notice their turn on mobile (where the app may be in background or not focused). Card grouping prevents the hand from becoming an unmanageable row of tiny cards on small screens, and sticky play buttons prevent the most important action from being pushed off screen.
Output: New useSoundEffects and useCardGrouping composables, updated TurnBanner with sound, updated PlayerCards with mobile grouping layout.
</objective>

<execution_context>
@/Users/stephendickson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stephendickson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-mobile-ux-improvements/15-RESEARCH.md
@.planning/phases/15-mobile-ux-improvements/15-01-SUMMARY.md

@packages/client/src/composables/usePlayingPhase.ts
@packages/client/src/components/PlayerCards.vue
@packages/client/src/components/TurnBanner.vue
@packages/client/src/components/PlayingPhase.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create sound effects composable and integrate with TurnBanner</name>
  <files>
    packages/client/src/composables/useSoundEffects.ts
    packages/client/src/components/TurnBanner.vue
  </files>
  <action>
    Create `packages/client/src/composables/useSoundEffects.ts`:
    - Export function `useSoundEffects()`
    - Maintain module-level `audioInitialized` boolean (outside function, shared across instances) and `turnSound: HTMLAudioElement | null`
    - `initializeAudio()`: If not initialized, create `new Audio()` element. Set its src to a data URI of a short notification beep (use a base64-encoded ~200ms sine wave beep — generate a minimal WAV data URI inline so no external file is needed). Call `.load()`. Set audioInitialized to true. This approach avoids needing to add a sound file to the assets folder.
    - Alternative: If base64 audio is too complex, use the Web Audio API to generate a simple beep programmatically: create AudioContext, create OscillatorNode (440Hz sine wave), connect to gain node (volume 0.3), play for 150ms. This is simpler and needs no file.
    - PREFERRED APPROACH: Use Web Audio API for programmatic beep generation. Create function `playBeep()` that:
      1. Creates AudioContext (or reuses existing one stored at module level)
      2. Creates OscillatorNode with frequency 880Hz (higher pitch = more alert-like), type 'sine'
      3. Creates GainNode with gain 0.15 (quiet but audible)
      4. Connects oscillator -> gain -> context.destination
      5. Starts oscillator at context.currentTime, stops at currentTime + 0.15 (150ms beep)
    - `playTurnNotification()`: Call `playBeep()`. Wrap in try/catch, log warning on failure.
    - On first call to playTurnNotification, AudioContext is created (satisfies mobile user gesture requirement since it's called from a watch triggered by user interaction flow)
    - Return { playTurnNotification }

    Update `packages/client/src/components/TurnBanner.vue`:
    - Import `useSoundEffects` from composables
    - Import `watch` from vue
    - In script setup: `const { playTurnNotification } = useSoundEffects()`
    - Add watch on `props.visible`: when it transitions to `true`, call `playTurnNotification()`
    - Keep all existing template and style unchanged
  </action>
  <verify>Run `make type-check` and `make lint` — both must pass.</verify>
  <done>When the "YOUR TURN" banner appears, a short 150ms beep sound plays. Sound uses Web Audio API (no external audio files needed). AudioContext created lazily on first notification. Sound fails gracefully on browsers that block audio.</done>
</task>

<task type="auto">
  <name>Task 2: Create card grouping composable and update PlayerCards with mobile layout</name>
  <files>
    packages/client/src/composables/useCardGrouping.ts
    packages/client/src/components/PlayerCards.vue
    packages/client/src/components/PlayingPhase.vue
  </files>
  <action>
    Create `packages/client/src/composables/useCardGrouping.ts`:
    - Import type { Card } from '@shit-head/shared' and { computed, ref, type Ref } from 'vue'
    - Export interface CardGroup { rank: string; count: number; cards: Card[]; indices: number[] }
    - Export function `useCardGrouping(hand: Ref<Card[]>)`
    - Computed `groupedCards`: iterate hand, group by rank (standard: card.rank, joker: 'JKR'). Return CardGroup[] sorted by the order cards appear in hand (first occurrence of each rank determines group position)
    - Ref `selectedCounts`: Record<string, number> initialized to {}
    - `incrementSelection(rank: string)`: increment selectedCounts[rank] up to max group.count. Ensure reactivity by using spread or Object.assign
    - `decrementSelection(rank: string)`: decrement selectedCounts[rank] down to 0
    - Computed `selectedIndices`: for each rank with selectedCounts > 0, take the first N indices from that rank's group. Return as number[]
    - Computed `hasGroupSelection`: selectedIndices.length > 0
    - `clearGroupSelection()`: reset selectedCounts to {}
    - `getSelectedCount(rank: string)`: return selectedCounts[rank] || 0
    - Return { groupedCards, selectedCounts, incrementSelection, decrementSelection, selectedIndices, hasGroupSelection, clearGroupSelection, getSelectedCount }

    Update `packages/client/src/components/PlayerCards.vue`:
    - Import useCardGrouping from composables
    - Import { computed, ref } from 'vue' (if not already)
    - Add a reactive `isMobile` detection: `const windowWidth = ref(window.innerWidth)` with a resize listener in onMounted/onUnmounted. `const isMobile = computed(() => windowWidth.value < 640)`
    - Initialize useCardGrouping with the hand prop: need to convert the prop to a computed ref. Use `const handRef = computed(() => props.hand)` then pass to `useCardGrouping(handRef)`
    - When isMobile is true AND hand has > 5 cards (threshold for grouping), render a GROUPED view instead of the individual card buttons:
      - Each group row: flex row with sample card visual (small 10x15 card showing rank+suit), rank label ("2x 7s"), and +/- buttons
      - +/- buttons: circular 32px buttons with bg-gray-600 text-white, increment/decrement the selection count for that rank
      - Show selected count between the +/- buttons
      - Style groups: bg-gray-800/50 rounded-lg p-2, gap-2 between groups
    - When isMobile is false OR hand has <= 5 cards, keep the existing individual card button layout unchanged
    - Make the action buttons section sticky: wrap the action buttons div in a `<div class="sticky bottom-0 bg-green-900/95 backdrop-blur-sm py-2 -mx-2 px-2">` so the Play and Pick Up Pile buttons remain visible when scrolling through grouped cards
    - When in grouped mode, the "Play" button should use the grouped selectedIndices instead of selectedHandIndices:
      - Add a new emit: 'play-grouped-cards' with indices: number[]
      - The Play button in grouped mode emits 'play-grouped-cards' with the useCardGrouping's selectedIndices
    - When in non-grouped mode, keep existing behavior unchanged

    Update `packages/client/src/components/PlayingPhase.vue`:
    - Handle the new 'play-grouped-cards' event from PlayerCards:
      - Add handler that calls `send({ type: 'play-cards', cardIndices: indices })` directly
      - Import send from the composable (it's already available via usePlayingPhase)
    - Actually, better approach: Instead of a new emit, modify PlayerCards to call the EXISTING 'play-cards' emit but first update selectedHandIndices to match the grouped selection. OR simpler: make PlayerCards emit 'play-cards' in both modes, and in grouped mode, before emitting, set the selectedHandIndices to match grouped selectedIndices.
    - SIMPLEST approach: In PlayerCards, when in grouped mode, the Play button click should:
      1. Emit 'play-grouped-cards' with the array of indices from useCardGrouping.selectedIndices
      2. Then call clearGroupSelection()
    - In PlayingPhase.vue, handle @play-grouped-cards by sending play-cards message directly:
      ```
      const handleGroupedPlay = (indices: number[]) => {
        send({ type: 'play-cards', cardIndices: indices });
      };
      ```
      Where `send` comes from useGameSocket (accessed through usePlayingPhase — check if send is returned, if not, destructure it from useGameSocket directly or add it to usePlayingPhase return)
    - Check usePlayingPhase.ts: it uses `const { send, ... } = useGameSocket()` but does NOT return `send`. Rather than modifying usePlayingPhase, import useGameSocket in PlayingPhase.vue to get send: `const { send } = useGameSocket()` — this is safe because useGameSocket is a singleton.
    - Wire: `<PlayerCards ... @play-grouped-cards="handleGroupedPlay" />`

    The player cards section in PlayingPhase.vue should allow scrolling when content overflows on mobile:
    - Change the bottom player cards wrapper from `class="flex-shrink-0 px-2 pb-3"` to `class="flex-shrink-0 px-2 pb-3 max-h-[45vh] overflow-y-auto"` so that if cards take up too much space, the area scrolls but is capped at 45% of viewport height.
  </action>
  <verify>Run `make type-check` and `make lint` — both must pass. Run `make build` to ensure production build succeeds.</verify>
  <done>On mobile screens (< 640px) with > 5 hand cards, cards display as grouped rows by rank with +/- quantity selectors. Play and Pick Up buttons are sticky at bottom, always accessible. Desktop layout unchanged. Card grouping correctly maps selected counts to card indices for playing.</done>
</task>

</tasks>

<verification>
1. `make type-check` passes with zero errors
2. `make lint` passes with zero errors
3. `make build` succeeds
</verification>

<success_criteria>
- Short beep sound plays when "YOUR TURN" banner appears (Web Audio API, no external files)
- Sound fails gracefully if browser blocks audio (no errors thrown)
- Hand cards grouped by rank on mobile (< 640px) when > 5 cards in hand
- Each group shows rank, count, sample card, and +/- selectors
- Selected group cards map correctly to indices for play-cards message
- Play and Pick Up Pile buttons remain visible (sticky) even with many card groups
- Player card area has max-height with scroll on mobile to prevent pushing content off screen
- Desktop layout unchanged (individual cards, no grouping)
</success_criteria>

<output>
After completion, create `.planning/phases/15-mobile-ux-improvements/15-02-SUMMARY.md`
</output>
