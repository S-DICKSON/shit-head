---
phase: 11-game-feedback-turn-indicators
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/client/src/components/TurnBanner.vue
  - packages/client/src/components/PlayingPhase.vue
  - packages/client/src/components/OpponentCards.vue
  - packages/client/src/components/PlayerCards.vue
autonomous: true

must_haves:
  truths:
    - "When it becomes my turn, a prominent 'YOUR TURN' banner appears on screen"
    - "When it is an opponent's turn, I can see which opponent is highlighted at a glance"
    - "Turn transitions between players have smooth visual transitions (not abrupt show/hide)"
    - "Screen readers announce turn changes via ARIA live region"
    - "Users with prefers-reduced-motion see no animations (instant state changes)"
  artifacts:
    - path: "packages/client/src/components/TurnBanner.vue"
      provides: "Prominent YOUR TURN overlay/banner with Vue Transition"
      min_lines: 20
    - path: "packages/client/src/components/PlayingPhase.vue"
      provides: "Turn change watcher, ARIA live region, TurnBanner integration"
      contains: "aria-live"
    - path: "packages/client/src/components/OpponentCards.vue"
      provides: "Enhanced turn indicator with smooth transitions"
      contains: "transition"
    - path: "packages/client/src/components/PlayerCards.vue"
      provides: "Removed duplicate Your Turn text (moved to TurnBanner)"
  key_links:
    - from: "packages/client/src/components/PlayingPhase.vue"
      to: "TurnBanner.vue"
      via: "isMyTurn prop"
      pattern: "isMyTurn"
    - from: "packages/client/src/components/PlayingPhase.vue"
      to: "ARIA live region"
      via: "watch on currentPlayerIndex"
      pattern: "aria-live"
---

<objective>
Add prominent turn feedback so players immediately know whose turn it is.

Purpose: The current UI has a small "Your Turn!" text buried above table cards and basic opponent highlighting. Players need unmistakable visual cues for turn changes — a prominent banner when it's their turn, smooth transitions on opponent indicators, and accessibility support via ARIA live regions.

Output: TurnBanner.vue component, enhanced PlayingPhase/OpponentCards/PlayerCards with turn feedback polish.
</objective>

<execution_context>
@/Users/stephendickson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stephendickson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@packages/client/src/components/PlayingPhase.vue
@packages/client/src/components/PlayerCards.vue
@packages/client/src/components/OpponentCards.vue
@packages/client/src/components/TurnTimer.vue
@packages/client/src/composables/usePlayingPhase.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TurnBanner component and integrate into PlayingPhase</name>
  <files>
    packages/client/src/components/TurnBanner.vue
    packages/client/src/components/PlayingPhase.vue
    packages/client/src/components/PlayerCards.vue
  </files>
  <action>
    **Create `TurnBanner.vue`:**
    A prominent "YOUR TURN" banner that appears when `isMyTurn` is true.

    Props: `visible: boolean`

    Structure:
    - Use Vue `<Transition>` with name="banner" wrapping the banner div
    - Banner: fixed or absolute positioned overlay near the top-center of the game area (below the timer, above opponents)
    - Style: bg-yellow-400 text-black font-bold text-lg px-6 py-2 rounded-full shadow-lg
    - Add a subtle scale entrance: banner-enter-from scale-90 opacity-0, banner-enter-to scale-100 opacity-100, banner-leave-to scale-90 opacity-0
    - Transition duration: 300ms ease-out for enter, 200ms ease-in for leave
    - Use `@media (prefers-reduced-motion: reduce)` to disable transitions (set duration to 0ms)
    - Keep it simple — just text "YOUR TURN" with no animations beyond the enter/leave transition

    **Update `PlayingPhase.vue`:**
    1. Import and add `TurnBanner` component between the timer area and the center game area
    2. Pass `:visible="isMyTurn"` to TurnBanner
    3. Add a visually-hidden ARIA live region (`aria-live="polite"` + `role="status"`) at the bottom of the template
    4. Use a `ref` for `turnAnnouncement` (string) and `watch` on `gameView.value?.currentPlayerIndex` to update it:
       - If `isMyTurn.value` is true: set to "It's your turn"
       - Else: find the current player nickname from `roomState.value.players[currentPlayerIndex]` and set to "It's {nickname}'s turn"
    5. The ARIA live region renders `{{ turnAnnouncement }}` — screen readers will announce turn changes
    6. Style the ARIA sr-only div: `class="sr-only"` (absolute, w-px, h-px, overflow-hidden, clip)

    **Update `PlayerCards.vue`:**
    1. Remove the "Your Turn!" indicator div at lines 4-8 (the `v-if="isMyTurn"` div with animate-pulse). This is now handled by TurnBanner in PlayingPhase.
    2. The `isMyTurn` prop is still used for button disabled states, so keep it in the props interface.
    3. No other changes needed to PlayerCards.

    **IMPORTANT CONSTRAINTS:**
    - Do NOT add urgency colors or state changes to TurnTimer.vue — that is a locked decision from Phase 8
    - Do NOT add sound effects — skip @vueuse/sound for v1, keep it simple
    - Do NOT use will-change CSS property
    - Only animate transform and opacity (GPU-accelerated properties)
  </action>
  <verify>
    Run `make type-check` — zero errors.
    Run `make lint` — zero errors.
    Visually inspect: TurnBanner.vue exists with Transition, PlayingPhase imports it, PlayerCards no longer has "Your Turn!" div, ARIA live region present.
  </verify>
  <done>
    TurnBanner appears with smooth transition when isMyTurn is true, disappears when not. PlayerCards no longer shows duplicate "Your Turn!" text. ARIA live region announces turn changes. prefers-reduced-motion disables animations.
  </done>
</task>

<task type="auto">
  <name>Task 2: Enhance opponent turn indicator transitions</name>
  <files>
    packages/client/src/components/OpponentCards.vue
  </files>
  <action>
    **Enhance `OpponentCards.vue` turn indicator transitions:**

    The current component has `isCurrentTurn` conditional styling (yellow ring + pulse dot). Enhance with smooth CSS transitions so the indicator doesn't just pop in/out:

    1. The outer div already has `transition-all` class. This is good — it means the ring-2/ring-yellow-400 and bg-yellow-900/40 changes already animate. Verify this works smoothly.

    2. For the pulse dot (the `w-2 h-2 bg-yellow-400 rounded-full animate-pulse` span):
       - Wrap it in a `<Transition name="dot">` so it fades in/out instead of instant show/hide
       - CSS: `.dot-enter-active, .dot-leave-active { transition: opacity 200ms ease; }` `.dot-enter-from, .dot-leave-to { opacity: 0; }`
       - Use `v-if` inside the Transition (already using v-if)

    3. Add `@media (prefers-reduced-motion: reduce)` block:
       - Set `animate-pulse` to `animation: none` (Tailwind's motion-reduce variant: add `motion-reduce:animate-none` class to the pulse dot)
       - Set transition durations to 0ms for the dot transition

    4. Add a subtle nickname emphasis when it's the opponent's turn:
       - When `isCurrentTurn` is true, add `text-yellow-300 font-semibold` to the nickname span (in addition to existing classes)
       - When false, keep default `text-sm font-medium` styling
       - This provides an additional visual cue beyond the ring

    **Keep everything else unchanged** — face-up cards display, hidden card indicators, disconnect indicator all stay the same.
  </action>
  <verify>
    Run `make type-check` — zero errors.
    Run `make lint` — zero errors.
    Inspect OpponentCards.vue: Transition wrapping pulse dot, motion-reduce class, nickname emphasis on turn.
  </verify>
  <done>
    Opponent turn indicators have smooth enter/leave transitions on the pulse dot. Nickname text emphasizes during opponent's turn. prefers-reduced-motion respected. All existing opponent display functionality unchanged.
  </done>
</task>

</tasks>

<verification>
1. `make type-check` passes with zero errors
2. `make lint` passes with zero errors
3. TurnBanner.vue exists and uses Vue Transition component
4. PlayingPhase.vue imports TurnBanner and has ARIA live region with `aria-live="polite"`
5. PlayerCards.vue no longer has "Your Turn!" text (removed, moved to TurnBanner)
6. OpponentCards.vue has Transition on pulse dot and motion-reduce handling
7. No changes to TurnTimer.vue (locked — no urgency colors)
</verification>

<success_criteria>
- Prominent "YOUR TURN" banner appears/disappears with smooth transition when turn changes
- Opponent turn indicators transition smoothly (not abrupt show/hide)
- Screen readers announce whose turn it is via ARIA live region
- prefers-reduced-motion users see instant state changes with no animation
- All existing game functionality remains unchanged
- `make type-check` and `make lint` pass cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/11-game-feedback-turn-indicators/11-01-SUMMARY.md`
</output>
