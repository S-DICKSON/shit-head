---
phase: 04-pre-game-swap-phase
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/client/src/components/Lobby.vue
  - packages/shared/src/schemas/messages.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "After 3-second countdown reaches 0, players navigate from /lobby to /game screen"
    - "Swap phase UI is displayed with cards after navigation"
    - "No dead schemas exist in shared message types"
  artifacts:
    - path: "packages/client/src/components/Lobby.vue"
      provides: "Navigation to /game on game-dealt message"
      pattern: "msg.type === 'game-dealt'"
    - path: "packages/shared/src/schemas/messages.ts"
      provides: "Message schemas without dead types"
      pattern: "!gameStartedSchema"
  key_links:
    - from: "packages/server/src/websocket/handlers.ts"
      to: "packages/client/src/components/Lobby.vue"
      via: "game-dealt message triggers navigation"
      pattern: "type: 'game-dealt'.*router\\.push"
---

<objective>
Fix lobby-to-swap-phase navigation by changing Lobby.vue to navigate on 'game-dealt' instead of 'game-started' and remove dead schema.

Purpose: The server replaced 'game-started' with per-player 'game-dealt' messages in Phase 3, but Lobby.vue was not updated to listen for the new message. This causes the countdown to complete but navigation never fires, blocking all swap phase tests.

Output: Players successfully navigate from lobby to swap phase screen after countdown completes.
</objective>

<execution_context>
@/Users/stephendickson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stephendickson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Gap source
@.planning/phases/04-pre-game-swap-phase/04-UAT.md
@.planning/debug/lobby-navigation-failure.md

# Prior phase context
@.planning/phases/04-pre-game-swap-phase/04-03-SUMMARY.md
@.planning/phases/04-pre-game-swap-phase/04-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Change Lobby.vue to navigate on game-dealt instead of game-started</name>
  <files>packages/client/src/components/Lobby.vue</files>
  <action>
Update the onMessage handler in Lobby.vue (lines 69-72) to listen for 'game-dealt' instead of 'game-started':

**Current (broken):**
```typescript
if (msg.type === 'game-started') {
  countdown.value = null;
  router.push('/game');
}
```

**Fixed:**
```typescript
if (msg.type === 'game-dealt') {
  countdown.value = null;
  router.push('/game');
}
```

**Why this works:**
- In Phase 3, the server replaced the generic 'game-started' message with per-player 'game-dealt' messages (see handlers.ts lines 262-282)
- game-dealt is sent to each player after the 3-second countdown completes (handlers.ts line 213)
- useGameSocket already handles game-dealt to set gameView state (useGameSocket.ts lines 91-106)
- Lobby.vue just needs to trigger navigation on the same message

**Why game-dealt not game-started:**
- game-started was intentionally removed in Phase 3 (see STATE.md line 128: "game-started replaced by game-dealt")
- game-dealt carries per-player card views, game-started was a generic signal
- The server never sends game-started anymore (grep confirms: only game-dealt is sent)
  </action>
  <verify>
1. Check the change is correct:
```bash
grep -A3 "msg.type === 'game-dealt'" packages/client/src/components/Lobby.vue
```
Expected: Should show countdown.value = null and router.push('/game') inside the block

2. Verify no references to game-started remain in Lobby.vue:
```bash
grep -i "game-started" packages/client/src/components/Lobby.vue
```
Expected: No matches (should return empty)
  </verify>
  <done>Lobby.vue listens for 'game-dealt' message (not 'game-started') to trigger navigation to /game</done>
</task>

<task type="auto">
  <name>Remove dead gameStartedSchema from shared messages</name>
  <files>packages/shared/src/schemas/messages.ts</files>
  <action>
Remove the dead gameStartedSchema (lines 130-132) and its reference in the discriminated union (line 218):

**Remove these lines:**
```typescript
export const gameStartedSchema = z.object({
  type: z.literal('game-started'),
});
```

**And remove from serverMessageSchema discriminated union:**
```typescript
gameStartedSchema,  // <-- DELETE this line
```

**Why remove it:**
- No server code sends this message (grep confirms: handlers.ts only sends game-dealt)
- Lobby.vue no longer listens for it (Task 1 changes to game-dealt)
- Dead code creates confusion for future development
- The type still exists in the union but will never match a real message

**Keep these imports/exports unchanged:**
- All other schema exports stay (gameStartingSchema for countdown, gameDealtSchema for cards)
- The discriminated union pattern stays (just remove one entry)
  </action>
  <verify>
1. Confirm gameStartedSchema is removed:
```bash
grep -n "gameStartedSchema" packages/shared/src/schemas/messages.ts
```
Expected: No matches (should return empty)

2. Verify serverMessageSchema still compiles:
```bash
cd packages/shared && bun run type-check
```
Expected: No TypeScript errors

3. Check remaining game message schemas are intact:
```bash
grep -E "(gameStartingSchema|gameDealtSchema)" packages/shared/src/schemas/messages.ts
```
Expected: Both schemas still present (game-starting for countdown, game-dealt for cards)
  </verify>
  <done>gameStartedSchema removed from shared types, serverMessageSchema discriminated union updated, no dead types remain</done>
</task>

<task type="auto">
  <name>Test navigation flow end-to-end</name>
  <files>packages/client/src/components/Lobby.vue</files>
  <action>
Manually test the full lobby-to-swap navigation flow to verify the gap is closed:

**Test procedure:**
1. Start the development environment:
```bash
make dev
```

2. Open browser to http://localhost:5173

3. Create a room and copy the room code

4. Open a second browser tab (or incognito window), join the room with the code

5. As host, click "Start Game" button

6. Observe countdown: 3... 2... 1... 0...

7. Verify navigation occurs:
   - Both players should automatically navigate to /game route
   - Swap phase UI should display (cards visible, timer counting down)
   - No stuck countdown overlay

**Expected behavior:**
- Countdown completes and disappears
- App navigates to /game immediately after countdown reaches 0
- SwapPhase component renders with hand cards, face-up cards, opponent views
- Timer shows ~30s and counts down

**If navigation fails:**
- Check browser console for errors
- Check terminal for server-side errors
- Verify game-dealt message is sent (should see it in network tab WebSocket frames)

**Gap closure criteria:**
- Test 1 from UAT.md passes: "After starting game, players navigate from lobby to swap phase screen"
- All 8 skipped tests become testable (swap phase screen is now reachable)
  </action>
  <verify>
Manual verification is required (this is the actual UAT test):

1. Follow the test procedure above
2. Confirm both players navigate to /game after countdown
3. Confirm swap phase UI is visible with cards

If test passes, the gap is closed.
  </verify>
  <done>Manual test confirms: countdown completes and both players navigate to /game route showing swap phase UI</done>
</task>

</tasks>

<verification>
1. Code changes applied correctly:
   - Lobby.vue listens for 'game-dealt' not 'game-started'
   - gameStartedSchema removed from shared types
   - No TypeScript errors in affected packages

2. Navigation flow works end-to-end:
   - Host clicks "Start Game"
   - 3-second countdown displays and completes
   - Both players automatically navigate to /game
   - Swap phase UI renders with cards and timer

3. No regressions:
   - Countdown still displays correctly (game-starting message unchanged)
   - useGameSocket still handles game-dealt to set gameView (unchanged)
   - Game.vue route guard passes (gameView is set before navigation)
</verification>

<success_criteria>
- [ ] Lobby.vue onMessage handler changed from 'game-started' to 'game-dealt'
- [ ] gameStartedSchema removed from messages.ts
- [ ] serverMessageSchema discriminated union updated (game-started entry removed)
- [ ] Type-check passes for shared and client packages
- [ ] Manual test confirms navigation occurs after countdown completes
- [ ] UAT Test 1 passes: Players navigate from lobby to swap phase screen after countdown
- [ ] All 8 skipped tests are now testable (swap phase screen reachable)
</success_criteria>

<output>
After completion, create `.planning/phases/04-pre-game-swap-phase/04-06-SUMMARY.md`
</output>
