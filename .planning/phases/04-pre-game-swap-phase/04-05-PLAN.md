---
phase: 04-pre-game-swap-phase
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/client/src/router.ts
  - packages/client/src/components/Game.vue
  - packages/client/src/components/Lobby.vue
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "After dealing, players see the SwapPhase UI instead of a placeholder overlay"
    - "Timer countdown is visible to all players during the swap phase"
    - "Players can interact with swap phase cards (tap-tap selection works)"
  artifacts:
    - path: "packages/client/src/components/Game.vue"
      provides: "Game wrapper component that renders SwapPhase when phase is swapping"
      min_lines: 15
    - path: "packages/client/src/router.ts"
      provides: "Route for /game that renders Game.vue"
      contains: "/game"
    - path: "packages/client/src/components/Lobby.vue"
      provides: "Navigation to /game route when game-dealt message received"
      contains: "router.push"
  key_links:
    - from: "packages/client/src/components/Lobby.vue"
      to: "/game route"
      via: "router.push('/game') on game-dealt message"
      pattern: "router\\.push.*game"
    - from: "packages/client/src/components/Game.vue"
      to: "packages/client/src/components/SwapPhase.vue"
      via: "conditional render when gameView.phase === 'swapping'"
      pattern: "SwapPhase"
    - from: "packages/client/src/router.ts"
      to: "packages/client/src/components/Game.vue"
      via: "route definition"
      pattern: "Game\\.vue"
---

<objective>
Wire SwapPhase.vue into the application by creating a Game.vue wrapper component, adding a /game route, and navigating from Lobby when game-dealt is received.

Purpose: Close the single verification gap — SwapPhase.vue is complete but orphaned. Players currently see a "Game Started!" placeholder instead of the swap phase UI.

Output: Players transition from lobby to swap phase UI after dealing completes.
</objective>

<execution_context>
@/Users/stephendickson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stephendickson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-pre-game-swap-phase/04-04-SUMMARY.md
@packages/client/src/router.ts
@packages/client/src/components/Lobby.vue
@packages/client/src/components/SwapPhase.vue
@packages/client/src/composables/useGameSocket.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Game.vue wrapper and /game route</name>
  <files>
    packages/client/src/components/Game.vue
    packages/client/src/router.ts
  </files>
  <action>
    1. Create `packages/client/src/components/Game.vue`:
       - Import `useGameSocket` and destructure `gameView`
       - Import `SwapPhase` component
       - Import `useRouter` from vue-router
       - On mount, if `gameView.value` is null, redirect to `/` (guard against direct URL access)
       - Template: conditionally render `<SwapPhase />` when `gameView.value?.phase === 'swapping' || gameView.value?.phase === 'transitioning'`
       - For other phases (future: 'playing', 'finished'), show a placeholder `<div>` with text like "Game in progress..." — this will be replaced in Phase 5+
       - Keep it minimal — this is a routing shell, not a layout component

    2. Add `/game` route to `packages/client/src/router.ts`:
       - Add route `{ path: '/game', name: 'game', component: () => import('./components/Game.vue') }`
       - Place it after the `/room/:code` route
  </action>
  <verify>
    - `packages/client/src/components/Game.vue` exists and imports SwapPhase
    - `packages/client/src/router.ts` has a `/game` route pointing to Game.vue
    - Run: `cd packages/client && npx vue-tsc --noEmit` to verify no type errors
  </verify>
  <done>Game.vue renders SwapPhase when phase is 'swapping' or 'transitioning', and /game route is registered in router</done>
</task>

<task type="auto">
  <name>Task 2: Wire Lobby.vue to navigate to /game on game-dealt</name>
  <files>
    packages/client/src/components/Lobby.vue
  </files>
  <action>
    1. In the `onMessage` handler in Lobby.vue, find the block that handles `game-started` message (lines 70-74):
       ```
       if (msg.type === 'game-started') {
         countdown.value = null;
         showGameStarted.value = true;
         // TODO: Navigate to game view in Phase 3
       }
       ```
       Replace this with navigation to the game route:
       ```
       if (msg.type === 'game-started') {
         countdown.value = null;
         router.push('/game');
       }
       ```
       Note: The `game-dealt` message is already handled by `useGameSocket` which sets `gameView`. The `game-started` message triggers AFTER `game-dealt` and is the correct moment to navigate (game-started fires after countdown completes).

    2. Remove the `showGameStarted` ref declaration (line 13: `const showGameStarted = ref(false);`)

    3. Remove the "Game Started Placeholder" template block (lines 154-161):
       ```html
       <!-- Game Started Placeholder -->
       <div
         v-if="showGameStarted"
         class="fixed inset-0 bg-green-600 bg-opacity-90 flex items-center justify-center z-50"
       >
         <div class="text-white text-4xl font-bold">
           Game Started!
         </div>
       </div>
       ```
       Delete this entire block.
  </action>
  <verify>
    - Lobby.vue no longer has `showGameStarted` ref
    - Lobby.vue no longer has "Game Started!" overlay template
    - Lobby.vue calls `router.push('/game')` when `game-started` message received
    - No TODO comment about navigating to game view remains
    - Run: `cd packages/client && npx vue-tsc --noEmit` to verify no type errors
  </verify>
  <done>Lobby navigates to /game when game starts, removing placeholder overlay and TODO comment</done>
</task>

</tasks>

<verification>
1. Type check: `cd packages/client && npx vue-tsc --noEmit` — no errors
2. Build check: `cd packages/client && bun run build` — produces dist without errors
3. Manual flow verification: Start server + client, create room with 2+ players, start game → should navigate from lobby to SwapPhase UI with countdown timer visible
4. Confirm SwapPhase.vue renders (timer, cards, ready button all visible)
5. Confirm no "Game Started!" overlay appears
</verification>

<success_criteria>
- Players see SwapPhase UI (timer countdown, cards, ready button) after game starts — NOT a "Game Started!" placeholder
- /game route exists and renders Game.vue
- Game.vue conditionally renders SwapPhase when phase is 'swapping' or 'transitioning'
- Lobby.vue navigates to /game on game-started message
- No type errors, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/04-pre-game-swap-phase/04-05-SUMMARY.md`
</output>
