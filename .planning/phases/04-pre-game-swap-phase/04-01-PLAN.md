---
phase: 04-pre-game-swap-phase
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/schemas/messages.ts
  - packages/shared/src/types/messages.ts
  - packages/shared/src/types/game.ts
autonomous: true

must_haves:
  truths:
    - "swap-cards and ready-up client message schemas exist and validate correctly"
    - "swap-timer-tick, player-ready, swap-phase-complete, and swap-cards-updated server message schemas exist"
    - "GamePhase includes 'transitioning' phase for post-swap transition"
    - "ErrorCode includes INVALID_ACTION and PLAYER_NOT_FOUND codes"
    - "All new message types are inferred as TypeScript types via z.infer"
  artifacts:
    - path: "packages/shared/src/schemas/messages.ts"
      provides: "Swap-related Zod schemas added to client/server discriminated unions"
      contains: "swapCardsSchema"
    - path: "packages/shared/src/types/messages.ts"
      provides: "Inferred TypeScript types for all new message schemas"
      contains: "SwapCardsMessage"
    - path: "packages/shared/src/types/game.ts"
      provides: "Updated GamePhase type with 'transitioning'"
      contains: "transitioning"
  key_links:
    - from: "packages/shared/src/types/messages.ts"
      to: "packages/shared/src/schemas/messages.ts"
      via: "z.infer type inference"
      pattern: "z\\.infer<typeof swapCardsSchema>"
    - from: "packages/shared/src/schemas/messages.ts"
      to: "clientMessageSchema"
      via: "discriminated union member"
      pattern: "swapCardsSchema"
---

<objective>
Add swap-phase message schemas and types to the shared package.

Purpose: Establish the message protocol for the swap phase so server and client can communicate swap actions, ready state, timer ticks, and phase transitions with type safety and runtime validation.

Output: Extended Zod schemas in messages.ts, inferred types in messages.ts, updated GamePhase in game.ts.
</objective>

<execution_context>
@/Users/stephendickson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stephendickson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/shared/src/schemas/messages.ts
@packages/shared/src/types/messages.ts
@packages/shared/src/types/game.ts
@packages/shared/src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add swap-phase schemas and update discriminated unions</name>
  <files>packages/shared/src/schemas/messages.ts, packages/shared/src/types/messages.ts, packages/shared/src/types/game.ts</files>
  <action>
1. In `packages/shared/src/types/game.ts`:
   - Update `GamePhase` to include `'transitioning'`: `'dealing' | 'swapping' | 'transitioning' | 'playing' | 'finished'`

2. In `packages/shared/src/schemas/messages.ts`:
   - Add new error codes to errorSchema: `'INVALID_ACTION'` and `'PLAYER_NOT_FOUND'` (append to existing z.enum array)
   - Add client-to-server schemas:
     ```
     swapCardsSchema = z.object({
       type: z.literal('swap-cards'),
       handIndex: z.number().int().min(0).max(2),
       faceUpIndex: z.number().int().min(0).max(2),
     })
     readyUpSchema = z.object({
       type: z.literal('ready-up'),
     })
     ```
   - Add to `clientMessageSchema` discriminated union: `swapCardsSchema`, `readyUpSchema`
   - Add server-to-client schemas:
     ```
     swapTimerTickSchema = z.object({
       type: z.literal('swap-timer-tick'),
       timeRemaining: z.number().int().min(0).max(30),
     })
     playerReadySchema = z.object({
       type: z.literal('player-ready'),
       playerId: z.string(),
       readyPlayers: z.array(z.string()),
     })
     swapPhaseCompleteSchema = z.object({
       type: z.literal('swap-phase-complete'),
       reason: z.enum(['timer-expired', 'all-ready']),
     })
     swapCardsUpdatedSchema = z.object({
       type: z.literal('swap-cards-updated'),
       hand: z.array(cardSchema),
       faceUp: z.array(cardSchema),
       opponents: z.array(opponentViewSchema),
     })
     ```
   - Add all four to `serverMessageSchema` discriminated union
   - Export all new schemas
   - Update `gameDealtSchema` phase enum to include `'transitioning'`

3. In `packages/shared/src/types/messages.ts`:
   - Import new schemas: `swapCardsSchema`, `readyUpSchema`, `swapTimerTickSchema`, `playerReadySchema`, `swapPhaseCompleteSchema`, `swapCardsUpdatedSchema`
   - Add inferred types:
     ```
     SwapCardsMessage = z.infer<typeof swapCardsSchema>
     ReadyUpMessage = z.infer<typeof readyUpSchema>
     SwapTimerTickMessage = z.infer<typeof swapTimerTickSchema>
     PlayerReadyMessage = z.infer<typeof playerReadySchema>
     SwapPhaseCompleteMessage = z.infer<typeof swapPhaseCompleteSchema>
     SwapCardsUpdatedMessage = z.infer<typeof swapCardsUpdatedSchema>
     ```
   - Add `'INVALID_ACTION'` and `'PLAYER_NOT_FOUND'` to the `ErrorCode` type union
  </action>
  <verify>
Run `bun run --cwd packages/shared typecheck` (or `bunx tsc --noEmit` from shared package root). All types should compile without errors. If no typecheck script exists, run `bunx tsc --noEmit -p packages/shared/tsconfig.json` or equivalent.
  </verify>
  <done>
All swap-phase schemas exist in messages.ts, inferred types exist in messages.ts, GamePhase includes 'transitioning', ErrorCode includes 'INVALID_ACTION' and 'PLAYER_NOT_FOUND', and clientMessageSchema/serverMessageSchema include all new message types. TypeScript compiles without errors.
  </done>
</task>

</tasks>

<verification>
- `bun run typecheck` passes across all packages
- `swapCardsSchema.parse({ type: 'swap-cards', handIndex: 0, faceUpIndex: 2 })` succeeds
- `readyUpSchema.parse({ type: 'ready-up' })` succeeds
- `clientMessageSchema.parse({ type: 'swap-cards', handIndex: 1, faceUpIndex: 0 })` succeeds
- `serverMessageSchema.parse({ type: 'swap-timer-tick', timeRemaining: 15 })` succeeds
</verification>

<success_criteria>
- Six new Zod schemas (2 client, 4 server) are defined and exported
- All schemas are members of their respective discriminated unions
- Six corresponding TypeScript types are inferred and exported
- GamePhase includes 'transitioning'
- ErrorCode includes 'INVALID_ACTION' and 'PLAYER_NOT_FOUND'
- All existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-pre-game-swap-phase/04-01-SUMMARY.md`
</output>
