---
phase: 01-project-setup-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - packages/client/package.json
  - packages/client/tsconfig.json
  - packages/client/tsconfig.app.json
  - packages/client/tsconfig.node.json
  - packages/client/vite.config.ts
  - packages/client/vitest.config.ts
  - packages/client/index.html
  - packages/client/src/main.ts
  - packages/client/src/App.vue
  - packages/client/src/vite-env.d.ts
  - packages/client/src/style.css
  - packages/client/src/test/setup.ts
  - tsconfig.json
autonomous: true

must_haves:
  truths:
    - "Vue dev server starts and serves a page in the browser on port 5173"
    - "Tailwind CSS utility classes render correctly"
    - "Client can import types from @shit-head/shared"
    - "Vitest runs and passes a sample test"
  artifacts:
    - path: "packages/client/package.json"
      provides: "Client package config with Vue, Vite, Tailwind, Vitest dependencies"
      contains: "vue"
    - path: "packages/client/vite.config.ts"
      provides: "Vite configuration with Vue plugin"
      contains: "@vitejs/plugin-vue"
    - path: "packages/client/vitest.config.ts"
      provides: "Vitest configuration for Vue component testing"
      contains: "jsdom"
    - path: "packages/client/src/App.vue"
      provides: "Root Vue component with Tailwind classes"
      contains: "class="
    - path: "packages/client/index.html"
      provides: "HTML entry point for Vite"
      contains: "main.ts"
    - path: "packages/client/src/style.css"
      provides: "Tailwind CSS directives"
      contains: "@tailwind"
  key_links:
    - from: "packages/client/package.json"
      to: "packages/shared/package.json"
      via: "workspace:* dependency protocol"
      pattern: "workspace:\\*"
    - from: "packages/client/src/App.vue"
      to: "packages/shared/src/index.ts"
      via: "import from @shit-head/shared"
      pattern: "@shit-head/shared"
    - from: "packages/client/vite.config.ts"
      to: "packages/client/src/main.ts"
      via: "Vite entry point resolution"
---

<objective>
Create the Vue 3 client package with Vite, Tailwind CSS, and Vitest configured.

Purpose: The frontend application that players will interact with. Establishing the client build pipeline now means every subsequent UI phase can focus purely on features.
Output: Working Vue 3 app served by Vite with hot reload, Tailwind CSS utility classes, and Vitest test runner.
</objective>

<execution_context>
@/Users/stephendickson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stephendickson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-setup-foundation/01-CONTEXT.md
@.planning/phases/01-project-setup-foundation/01-RESEARCH.md
@.planning/phases/01-project-setup-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vue 3 client with Vite and Tailwind CSS</name>
  <files>
    packages/client/package.json
    packages/client/tsconfig.json
    packages/client/tsconfig.app.json
    packages/client/tsconfig.node.json
    packages/client/vite.config.ts
    packages/client/index.html
    packages/client/src/main.ts
    packages/client/src/App.vue
    packages/client/src/vite-env.d.ts
    packages/client/src/style.css
    packages/client/public/.gitkeep
    tsconfig.json
  </files>
  <action>
Create the Vue 3 client package manually (do NOT use create-vue — we need precise control over the config):

1. **packages/client/package.json**:
   - name: "@shit-head/client"
   - version: "0.0.1"
   - type: "module"
   - scripts: { "dev": "vite", "build": "vue-tsc --noEmit && vite build", "preview": "vite preview", "test": "vitest run", "test:watch": "vitest", "type-check": "vue-tsc --noEmit", "lint": "eslint ." }
   - dependencies: { "vue": "^3.5.0", "@shit-head/shared": "workspace:*" }
   - devDependencies: { "@vitejs/plugin-vue": "latest", "vite": "^5.0.0", "vue-tsc": "latest", "typescript": "^5.0.0", "tailwindcss": "^4.0.0", "@tailwindcss/vite": "^4.0.0", "vitest": "^2.0.0", "@vue/test-utils": "latest", "jsdom": "latest" }

2. **packages/client/vite.config.ts**:
   ```typescript
   import { defineConfig } from 'vite'
   import vue from '@vitejs/plugin-vue'
   import tailwindcss from '@tailwindcss/vite'

   export default defineConfig({
     plugins: [vue(), tailwindcss()],
     server: {
       host: '0.0.0.0',  // Required for Docker
       port: 5173,
       proxy: {
         '/api': 'http://localhost:3000',
         '/ws': { target: 'ws://localhost:3000', ws: true }
       }
     }
   })
   ```
   Note: Tailwind CSS v4 uses a Vite plugin instead of PostCSS. No tailwind.config.ts needed — configuration is done via CSS.

3. **packages/client/tsconfig.json**:
   - References to tsconfig.app.json and tsconfig.node.json
   - files: [] (references-only config)

4. **packages/client/tsconfig.app.json**:
   - extends: "@vue/tsconfig/tsconfig.dom.json" (install @vue/tsconfig as devDependency)
   - include: ["src/**/*", "src/**/*.vue"]
   - compilerOptions: { composite: true, baseUrl: ".", paths: { "@/*": ["./src/*"] } }

5. **packages/client/tsconfig.node.json**:
   - extends: "@vue/tsconfig/tsconfig.dom.json"
   - include: ["vite.config.ts", "vitest.config.ts"]
   - compilerOptions: { composite: true, module: "ESNext" }

6. **packages/client/index.html**:
   - Standard Vite HTML entry with `<div id="app"></div>` and `<script type="module" src="/src/main.ts"></script>`
   - Title: "Shithead Online"

7. **packages/client/src/main.ts**:
   - Import './style.css'
   - Import { createApp } from 'vue'
   - Import App from './App.vue'
   - createApp(App).mount('#app')

8. **packages/client/src/App.vue**:
   - Simple `<script setup lang="ts">` component
   - Import APP_VERSION from '@shit-head/shared' to verify workspace linking
   - Template with Tailwind classes: a centered card showing "Shithead Online" title and version
   - Use classes like: "min-h-screen bg-green-900 flex items-center justify-center"
   - Card: "bg-white rounded-2xl shadow-xl p-8 text-center"
   - Title: "text-4xl font-bold text-gray-800"
   - Subtitle: "text-gray-500 mt-2" showing "v{{ version }}"

9. **packages/client/src/vite-env.d.ts**:
   - `/// <reference types="vite/client" />`

10. **packages/client/src/style.css**:
    - `@import "tailwindcss";`
    (Tailwind v4 uses this single import instead of the old @tailwind directives)

11. **packages/client/public/.gitkeep**: Empty file to preserve directory.

12. Update **root tsconfig.json** references to include packages/client.

13. Run `bun install` from root to install all new dependencies and link workspaces.

Important: Add @vue/tsconfig to client devDependencies. If Tailwind v4 Vite plugin has a different import path, check the installed package exports and adjust.
  </action>
  <verify>
    Run: `bun install` at root succeeds.
    Run: `cd packages/client && bunx vite --host` — Vite dev server starts on port 5173.
    Open: http://localhost:5173 — page loads with "Shithead Online" title and green background.
    Run: `cd packages/client && bunx vue-tsc --noEmit` — TypeScript compiles without errors.
    Kill the dev server after verification.
  </verify>
  <done>Vue 3 client app serves on port 5173 with Vite hot reload. Tailwind CSS classes render correctly (green background, white card, styled text). Shared package import works (version displays). TypeScript compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Configure Vitest for Vue component testing</name>
  <files>
    packages/client/vitest.config.ts
    packages/client/src/test/setup.ts
    packages/client/src/components/__tests__/App.test.ts
  </files>
  <action>
Set up Vitest with Vue component testing support:

1. **packages/client/vitest.config.ts**:
   ```typescript
   import { defineConfig } from 'vitest/config'
   import vue from '@vitejs/plugin-vue'

   export default defineConfig({
     plugins: [vue()],
     test: {
       globals: true,
       environment: 'jsdom',
       setupFiles: ['./src/test/setup.ts'],
       include: ['src/**/*.{test,spec}.{ts,tsx}']
     }
   })
   ```

2. **packages/client/src/test/setup.ts**:
   - Empty for now, with comment: "// Global test setup — add shared mocks, custom matchers here"

3. **packages/client/src/components/__tests__/App.test.ts**:
   - Import { mount } from '@vue/test-utils'
   - Import App from '../../App.vue'
   - Test: "renders the app title" — mount App, assert it contains text "Shithead Online"
   - Test: "displays the version from shared package" — mount App, assert it contains "0.0.1"

Also create a basic server test to verify the test runner works for server-side code:

4. **packages/server/src/__tests__/health.test.ts**:
   - Import { describe, it, expect } from 'bun:test' (Bun's native test runner for server — simpler, no DOM needed)
   - Test: "health endpoint returns ok" — use fetch against http://localhost:3000/health (or test the response construction logic directly)
   - Since server tests are simpler (no DOM), using Bun's built-in test runner is fine for server package. Keep Vitest for client.
   - Actually, for consistency per research recommendation, use Vitest everywhere. Add vitest to server devDependencies.
   - Create **packages/server/vitest.config.ts** with simple config (no vue plugin, no jsdom — just node environment)
   - Update packages/server/package.json scripts: "test": "vitest run"

Run the tests to verify.
  </action>
  <verify>
    Run: `cd packages/client && bunx vitest run` — tests pass (2 passing).
    Run: `cd packages/server && bunx vitest run` — tests pass (or skip if no meaningful tests yet — the health test may need server running).
    All tests produce green output.
  </verify>
  <done>Vitest is configured for Vue component testing in client package. Sample tests pass, confirming component mounting and shared package integration work. Server package also has Vitest configured for future test needs.</done>
</task>

</tasks>

<verification>
1. `bun install` at root resolves all workspace dependencies including client
2. `bunx --cwd packages/client vite` starts dev server on port 5173
3. Browser shows styled Vue app with Tailwind CSS and shared package version
4. `bunx --cwd packages/client vue-tsc --noEmit` passes type checking
5. `bunx --cwd packages/client vitest run` passes all tests
6. Root tsconfig references all three workspace packages
</verification>

<success_criteria>
- Vue 3 + Vite dev server starts with hot module replacement
- Tailwind CSS v4 utility classes render correctly
- Client imports from @shit-head/shared work at runtime
- Vitest runs component tests successfully
- TypeScript compiles without errors across all packages
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-setup-foundation/01-02-SUMMARY.md`
</output>
