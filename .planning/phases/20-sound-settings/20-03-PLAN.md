---
phase: 20-sound-settings
plan: 03
type: execute
wave: 2
depends_on: ["20-01", "20-02"]
files_modified:
  - packages/client/src/components/Lobby.vue
  - packages/client/src/components/DiscordLobby.vue
  - packages/client/src/components/PlayingPhase.vue
  - packages/client/src/composables/useGameSocket.ts
autonomous: true

must_haves:
  truths:
    - "Host sees round time selector (30s, 45s, 60s) in lobby before game starts"
    - "Non-host sees current round time as read-only text"
    - "Clicking a round time button sends set-round-time message to server"
    - "TurnTimer displays correct total time based on room's configured roundTime"
    - "Round time selector works in both Lobby.vue and DiscordLobby.vue"
    - "MuteButton visible during PlayingPhase"
  artifacts:
    - path: "packages/client/src/components/Lobby.vue"
      provides: "Round time selector for host, read-only display for non-host"
      contains: "set-round-time"
    - path: "packages/client/src/components/DiscordLobby.vue"
      provides: "Round time selector for host (Discord variant)"
      contains: "set-round-time"
    - path: "packages/client/src/components/PlayingPhase.vue"
      provides: "Dynamic total-time from roomState.roundTime, MuteButton"
      contains: "roundTime"
    - path: "packages/client/src/composables/useGameSocket.ts"
      provides: "return-to-lobby handler uses message.room.roundTime for turnTimeRemaining reset"
      contains: "roundTime"
  key_links:
    - from: "packages/client/src/components/Lobby.vue"
      to: "packages/server/src/websocket/handlers.ts"
      via: "send({ type: 'set-round-time', roundTime })"
      pattern: "set-round-time"
    - from: "packages/client/src/components/PlayingPhase.vue"
      to: "packages/client/src/composables/useGameSocket.ts"
      via: "roomState.roundTime passed to TurnTimer total-time"
      pattern: "roundTime"
---

<objective>
Wire configurable round time into client lobby views and game timer, plus add MuteButton to PlayingPhase.

Purpose: Complete the round time feature by adding the host UI controls (SETS-02) and making the turn timer use the configured value (SETS-03). Also adds MuteButton to PlayingPhase (deferred from Plan 01 to avoid file conflicts).
Output: Lobby round time selectors, dynamic TurnTimer, MuteButton in PlayingPhase.
</objective>

<execution_context>
@/Users/stephendickson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stephendickson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-sound-settings/20-RESEARCH.md
@.planning/phases/20-sound-settings/20-01-SUMMARY.md
@.planning/phases/20-sound-settings/20-02-SUMMARY.md
@packages/client/src/components/Lobby.vue
@packages/client/src/components/DiscordLobby.vue
@packages/client/src/components/PlayingPhase.vue
@packages/client/src/composables/useGameSocket.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add round time selector to both lobby components</name>
  <files>
    packages/client/src/components/Lobby.vue
    packages/client/src/components/DiscordLobby.vue
  </files>
  <action>
**Lobby.vue** — Add round time selector for host:

1. Add a `setRoundTime` function in the script section (after `leaveRoom`):
   ```typescript
   const setRoundTime = (time: 30 | 45 | 60) => {
     send({ type: 'set-round-time', roundTime: time });
   };
   ```

2. In the template, inside the host controls section (`v-if="isHost"`), add a round time selector **before** the Start Game button:
   ```html
   <div class="mb-4">
     <label class="block text-sm font-medium text-gray-600 mb-2">Round Time</label>
     <div class="flex gap-2">
       <button
         v-for="time in [30, 45, 60]"
         :key="time"
         :class="[
           'px-4 py-2 rounded-lg text-sm font-medium border transition-all',
           roomState?.roundTime === time
             ? 'bg-green-600 text-white border-green-600'
             : 'bg-white text-gray-700 border-gray-300 hover:bg-gray-50'
         ]"
         @click="setRoundTime(time as 30 | 45 | 60)"
       >
         {{ time }}s
       </button>
     </div>
   </div>
   ```

3. In the non-host section (`v-else` after host controls), change from just "Waiting for host to start..." to also show the current round time:
   ```html
   <div v-else class="mt-6 text-center text-gray-600 italic">
     <p>Waiting for host to start...</p>
     <p v-if="roomState?.roundTime" class="text-sm mt-1">
       Round time: {{ roomState.roundTime }}s
     </p>
   </div>
   ```

**DiscordLobby.vue** — Add same round time selector for host:

1. Add the same `setRoundTime` function in the script section:
   ```typescript
   const setRoundTime = (time: 30 | 45 | 60) => {
     send({ type: 'set-round-time', roundTime: time });
   };
   ```

2. In the template, inside the host controls (`v-if="isHost"`), add the round time selector **before** the Start Game button. Use the same HTML structure as Lobby.vue.

3. In the non-host waiting message section, add the read-only round time display (same as Lobby.vue).
  </action>
  <verify>
Run `make type-check` — no TypeScript errors.
Verify both Lobby.vue and DiscordLobby.vue contain `set-round-time` in their script.
  </verify>
  <done>
Host sees 30s/45s/60s toggle buttons in lobby. Selected button is highlighted green. Non-host sees current round time as read-only text. Both web and Discord lobbies have the selector.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire round time to TurnTimer, add MuteButton to PlayingPhase, update useGameSocket</name>
  <files>
    packages/client/src/components/PlayingPhase.vue
    packages/client/src/composables/useGameSocket.ts
  </files>
  <action>
**PlayingPhase.vue** — Two changes:

1. **Add MuteButton** (deferred from Plan 01):
   - Import: `import MuteButton from './MuteButton.vue';`
   - Add `<MuteButton />` in the template, between the Leave button and the Spectator count span. It will render as a fixed-position overlay (positioning is in MuteButton.vue itself).

2. **Replace hardcoded total-time**:
   - Find: `:total-time="45"`
   - Replace with: `:total-time="roomState?.roundTime ?? 45"`
   - The `roomState` ref is already available via `useGameSocket()` (already destructured at the top of the script).

**useGameSocket.ts** — Update return-to-lobby handler:

1. In the `case 'return-to-lobby':` handler, find the line:
   ```typescript
   turnTimeRemaining.value = 45;
   ```
   Replace with:
   ```typescript
   turnTimeRemaining.value = message.room.roundTime ?? 45;
   ```
   This ensures turnTimeRemaining resets to the room's configured round time, not the hardcoded 45. The `?? 45` fallback handles backward compatibility if roundTime is somehow missing.

No other changes needed — `turnTimeRemaining` initialization (`ref<number>(45)`) is fine as a safe default before any game starts. The server will send the correct value via timer ticks.
  </action>
  <verify>
Run `make type-check` — no TypeScript errors.
Run `make test-client` — all client tests pass.
Run `make lint` — no lint errors.
Verify PlayingPhase.vue contains `roundTime` in the TurnTimer binding.
Verify PlayingPhase.vue imports and renders MuteButton.
  </verify>
  <done>
TurnTimer uses roomState.roundTime for total-time (not hardcoded 45). MuteButton visible in PlayingPhase. useGameSocket return-to-lobby resets turnTimeRemaining to room's configured roundTime.
  </done>
</task>

</tasks>

<verification>
1. `make type-check` passes with zero errors
2. `make test-client` passes — all client tests pass
3. `make test-server` passes — all server tests pass
4. `make lint` passes with zero errors
5. Lobby.vue and DiscordLobby.vue both have round time selector with 30/45/60 options
6. PlayingPhase.vue TurnTimer uses `:total-time="roomState?.roundTime ?? 45"`
7. PlayingPhase.vue renders MuteButton component
8. useGameSocket return-to-lobby handler uses `message.room.roundTime`
</verification>

<success_criteria>
- Host sees round time toggle buttons (30s, 45s, 60s) in both Lobby.vue and DiscordLobby.vue
- Selected round time button highlighted in green
- Non-host players see current round time as read-only text
- Clicking round time button sends set-round-time WebSocket message
- TurnTimer total-time is dynamic from roomState.roundTime
- MuteButton visible in PlayingPhase.vue
- return-to-lobby resets turnTimeRemaining to configured roundTime
- All tests pass, lint clean
</success_criteria>

<output>
After completion, create `.planning/phases/20-sound-settings/20-03-SUMMARY.md`
</output>
